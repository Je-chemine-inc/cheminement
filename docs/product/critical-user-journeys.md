# Critical user journeys (CUJs)

The flows that **must never break**. Inferred from the central routes and the matching/payment engines. Every row is **`[inferred — confirm]`** until the product owner confirms it.

**Rule going forward:** a change that touches a CUJ must **verify that journey** (run it, or run/extend its tests) before claiming done — see [/verify](../../.claude/skills/verify/SKILL.md). A new user-visible feature must extend or register a CUJ here. "Test coverage" below is the *existing* automated coverage; **"none"** means manual verification is the only safety net, so be extra careful (see [debt-map](../quality/debt-map.md)).

| ID | Journey | Steps (user's words) | Where it lives in code | Test coverage | Status |
| --- | --- | --- | --- | --- | --- |
| CUJ-1 | **Client intake → match** | "I pick who it's for, choose my reasons, submit — and get matched to a professional." | `src/app/appointment/page.tsx` → `POST /api/appointments` → `src/lib/appointment-routing.ts` (`routeAppointmentToProfessionals`) | matcher + cascade: `lib/appointment-routing.spec.ts`, `appointment-routing-gender.spec.ts`, `proposal-timeout.spec.ts`, `service-request.spec.ts`. **Route handler: none** | `[inferred — confirm]` |
| CUJ-2 | **Guest booking + guest payment** | "Without an account, I book and pay through the link I was sent." | `src/app/appointment` (guest) → `POST /api/appointments/guest` → `src/app/pay/page.tsx` (`?token=`) → `api/payments/guest*` | **none** (all guest payment routes untested — debt-map P2) | `[inferred — confirm]` |
| CUJ-3 | **Pro accepts a proposal / self-claims** | "I review the request and take the client (or pull one from the general pool)." | `src/app/(privilaged)/professional/dashboard/proposals/page.tsx` → `POST /api/appointments/[id]/accept` (+ `/refuse`) | cascade logic: `proposal-timeout.spec.ts`. **Accept/refuse routes: none** | `[inferred — confirm]` |
| CUJ-4 | **Schedule → payment-method capture** | "My pro sets a date; I'm invited to add a payment method to confirm." | `POST /api/appointments/[id]/schedule-first` → payment-invitation email → `/pay` or `billing?action=addPaymentMethod` → SetupIntent | `client-payment-guarantee.spec.ts`, `client-portal-urls.spec.ts`. **Setup routes: none** | `[inferred — confirm]` |
| CUJ-5 | **Session closure → charge → receipt** | "After my session I'm charged and get my official receipt." | `POST /api/appointments/[id]/complete-session` → `lib/session-closure.ts` (no-show billing) → `lib/stripe-off-session-charge.ts` / Interac → `issueFiscalReceipt` (`lib/session-post-closure.ts`) → receipt PDF | `complete-session/route.spec.ts`, `payment-settlement.spec.ts`, `session-post-closure.spec.ts`, `stripe-off-session-charge.spec.ts`, `pricing.spec.ts` ✅ | `[inferred — confirm]` |
| CUJ-6 | **Stripe webhook settlement** | (system) "Payment confirms → mark paid, issue receipt, green the guarantee." | `src/app/api/payments/webhook/route.ts` → `lib/payment-settlement.ts` | **none** — the webhook itself is untested (debt-map **P1**); depends on LIVE event subscriptions | `[inferred — confirm]` |
| CUJ-7 | **Professional payout** | "I get paid for my completed sessions." | `POST /api/stripe-connect/payout` and `api/admin/accounting/professionals/[id]/payout` → `ProfessionalLedgerEntry` debit | `stripe-connect/payout/route.spec.ts`, `admin/accounting/.../payout` spec ✅ | `[inferred — confirm]` |
| CUJ-8 | **Signup → verify → login** | "I register, confirm my email, confirm my phone, then sign in." | `src/app/(auth)/signup/member` → `POST /api/auth/signup` → `(verify)/verify-account` (email link → SMS) → `(auth)/login` | `auth/account/verify-email/route.spec.ts`, `auth/forgot-password/route.spec.ts`. **Signup route (618 lines): none** | `[inferred — confirm]` |
| CUJ-9 | **Account lifecycle (deactivate / RTBF)** | "I can deactivate my account or request permanent deletion." | settings pages → `POST /api/users/me/deactivate`, `POST /api/users/me/request-deletion` (admin alerted, financial data retained) | `users/me/deactivate` + `request-deletion` route specs ✅ | `[inferred — confirm]` |
| CUJ-10 | **Internal messaging** | "I message my professional / support and get a reply." | `src/components/inbox/InboxView.tsx` → `api/messages/*` (allow-list in `lib/messaging-permissions.ts`) | `api/messages/messaging-security.spec.ts` ✅ | `[inferred — confirm]` |
| CUJ-11 | **Admin triage queue** | "I assign, auto-match, release, or schedule a pending request." | `src/components/admin/RequestsQueueTable.tsx` → `api/admin/service-requests/[id]/{assign,approve,schedule}`, `/general-pool` | `service-requests/[id]/{assign,schedule}` specs; appointment `[id]` PATCH auth specs ✅ (partial) | `[inferred — confirm]` |
| CUJ-12 | **Public content & legal pages render** | "I browse the library and read the terms/privacy pages." | `(public)/{explore,approaches,nouveautes,medias}/[slug]`, `(public)/{terms,privacy,cookies}` → `ContentEntry` / `LegalDocument` via `lib/legal-content.ts` | `legal-sections.spec.ts` (parse/serialize). **Page render: none** | `[inferred — confirm]` |
| CUJ-13 | **Showcase page: invitation → publication** | "The team invites me; I fill in my page, pick my city, add my photo, accept the publication consent and send it; an admin approves it and it goes live at my city's address." (The city picked before the first publication moves the page at once; on a page that has been public, the move waits for the approval and the old address redirects.) | `/admin/dashboard/showcases` → `POST /api/admin/showcases` (invite) → `/professional/dashboard/showcase` → `PUT /api/professional/showcase`, `POST …/photo`, `POST …/submit` → `POST /api/admin/showcases/[userId]` (`approve`) → `psy<city>.jechemine.ca/<slug>` (`src/app/showcase/[cityKey]/[proSlug]`, routed by `src/middleware.ts`); rules in `lib/showcase-workflow.ts`, writes in `lib/showcase-service.ts`, the public object in `lib/showcase-public.ts` | `showcase-workflow`, `showcase-service`, `showcase-public` (poison fixture), `showcase-queries`, `showcase-messages`, `files/[id]` route, both route-guard sweeps ✅ | new (spec 003), dark behind `showcaseEnabled` |
| CUJ-14 | **Find a professional by city** | "I search « psychologue Mascouche », land on the city page, see who is presented and their expertises, open a professional's page, then request an appointment." | www `/psy` → `/psy/<region>` (`src/app/(public)/psy`) → `psy<city>.jechemine.ca/` and `/specialite/<slug>` (`src/app/showcase/[cityKey]`) → `/<slug>` → the booking funnel on www; rules in `lib/showcase-seo.ts`, reads in `lib/showcase-queries.ts`, visit counts through `POST /api/showcase/beacon` | `showcase-seo` (indexing decision, headings, directory, sitemaps, structured data), `showcase-queries`, `client-messages`, `middleware` + `showcase-hosts`, both sitemap specs, the beacon route spec ✅ | new (spec 003), dark behind `showcaseEnabled` |
| CUJ-15 | **Request a time on a professional's page** | "On the professional's page I pick a consultation and a free time, send my request, and the time is held for me; the professional confirms and I receive the confirmation and the payment instructions." | `psy<city>.jechemine.ca/<slug>` (`ShowcaseSlotPicker` → `GET /api/showcase/[slug]/slots`) → www `/appointment?pro=&service=&date=&time=` (`useShowcaseDirectRequest`, `GET /api/showcase/[slug]/summary`) → `POST /api/appointments` or `/guest` with `direct` (`lib/direct-request.ts`: re-check, hold, propose to that professional) → proposals « Demandes directes » → `POST /api/appointments/[id]/accept-direct` (accepted and scheduled in one claim, first-appointment confirmation from `lib/first-appointment-confirmation.ts`); free times in `lib/available-slots.ts` + `lib/slot-occupancy.ts`, the lock in `lib/slot-holds.ts`, prices in `lib/pricing.ts` (`quick`) | `available-slots`, `slot-occupancy`, `slot-holds`, `showcase-booking`, `direct-request`, `direct-request-rules`, `appointment-writable-fields`, `route.intake`, `accept-direct`, `schedule-first`, the slots, summary and available-slots route specs, `pricing` ✅ | new (spec 003), dark behind `showcaseEnabled` |
| CUJ-16 | **My requested time is not available** | "The professional cannot take my requested time, or did not answer in time: I get an email to pick another time on their page, or to let Je chemine match me. I can also withdraw a request that is still waiting." | `POST /api/appointments/[id]/decline-direct`, or the hourly `runProposalTimeouts` → `runDirectRequestTimeouts` → `routingStatus: "awaiting_admin"` + `direct_request_unavailable` email → www `/demande-directe/rejumeler?t=` → `POST /api/appointments/direct/reroute` → matcher; withdrawal through `PATCH /api/appointments/[id]` (client dashboard); the admin queue marks each state | `direct-request` (release, timeouts, reroute), `decline-direct`, the reroute route, `proposal-timeout` ✅ | new (spec 003), dark behind `showcaseEnabled` |
| CUJ-17 | **Wait for a professional** | "No time suits me on the professional's page, so I join their waitlist. When a time frees up I get an email (and a text if I agreed) and the time is held 15 minutes; I book it and my request goes to the professional. I can leave the list with the link in my email." | `psy<city>.jechemine.ca/<slug>` (`ShowcaseWaitlistForm` → `POST /api/showcase/[slug]/waitlist`, `lib/waitlist-entries.ts`) → a freed time (any route calling `afterSlotFreed`, or the `waitlist-offers` job every two minutes and `triggerDueWaitlistOffers`) → `lib/waitlist-offers.ts` (hold, then claim the entry, then email/text) → www `/la/<token>` → `/liste-attente/reclamer?t=` → `POST /api/waitlist/claim` (the offer's hold becomes a direct request, CUJ-15) ; leaving: `/liste-attente/quitter?t=` → `POST /api/waitlist/leave`; the professional's « Liste d'attente » and the team's « Listes d'attente » (`/api/professional/waitlist`, `/api/admin/waitlist`) | `waitlist-rules` (fit, queue order, text message, join form), `waitlist-offers`, `waitlist-entries`, `slot-holds` (conversion), `direct-request` (waitlist claim), the join, leave, offer, claim, cron, professional, admin and short-link route specs ✅ | new (spec 003), dark behind `showcaseEnabled` |
| CUJ-18 | **Sell a training or a digital product** | "As a professional I create a video, audio, PDF, webinar or external product, send it for review with my rights attestation, and once the team approves it, it is sold on the site and my share is credited to my balance; a refund or a dispute takes it back. As a buyer I keep access even if the product is taken down, and a webinar I bought is announced by email the day before and an hour before. A professional whose account is deactivated has their products leave the site at once." | `/professional/dashboard/products` → `api/professional/products/**` (`lib/products.ts`: drafts, sanitized text via `lib/product-html.ts`, files, submission) → `/admin/dashboard/products` → `POST /api/admin/products/[slug]` → `www/book/<slug>` (`resolveResourceAccess`, the PDF at `GET /api/products/[slug]/file`) → `POST /api/resources/[slug]/purchase-intent` (commission snapshotted) → Stripe webhook / `confirm` → `settleProductPurchase` → `lib/product-ledger.ts` (`ProfessionalLedgerEntry` rows `product:<entitlement>:<n>`); cards on the showcase page (`listShowcaseProducts`); webinar reminders and status upkeep in the `products` job (`api/cron/products` every ten minutes, `triggerDueProductJobs`, `lib/product-jobs.ts`); every route that changes a professional's account calls `syncProfessionalProducts` | `product-rules` (commission, bounds, moderation, media hosts, writable fields, reminder windows), `product-html` (XSS fixtures), `product-ledger` (sale, reversal, recredit, dispute, race), `products` (account sync, upkeep), `product-jobs` (reminders: who, claim, retry, links), `content-premium-products`, the professional, admin, file, upload and products cron route specs, `purchase-intent` (snapshot), the CMS `PRO_OWNED` guard, the product sync in the deactivate, account-activation, user editor (status and delete), validate and data-lifecycle route specs ✅ — the Stripe webhook itself stays untested (debt-map P1) | new (spec 003) |

## Highest-risk, least-covered (verify manually with extra care)

- **CUJ-6 (Stripe webhook)** — untested and money-critical; also deploy-coupled to LIVE event subscriptions.
- **CUJ-2 (guest payment)** — entirely untested money path.
- **CUJ-1 / CUJ-8** — the matcher/cascade *logic* is well tested, but the booking and signup **route handlers** (the largest in the app) are not.

When you touch any of these, run the relevant `pnpm test` specs **and** exercise the flow in a running app (`pnpm dev`), per [/verify](../../.claude/skills/verify/SKILL.md).
