# Debt map

The honest map of landmines, fragile areas, missing coverage, and deploy-coupled steps in **Je chemine**. **This file is append-only**: future sessions *add* findings here (with a date) instead of fixing-by-the-way. Before a non-trivial change, check whether you're about to touch something on this list.

Severity: **P1** = can lose money / data / security now · **P2** = real risk, handle with care · **P3** = cruft / hygiene.

## Security & production landmines

- **[P1] Weak super-admin in the real DB.** `admin@admin.com` / `admin123` (full super-admin) is reported to exist in the production database (`RAPPORT-FEEDBACK-JUIN-2026.md` → "Points d'attention" #1). Must be changed/removed before/at production. *(observed: RAPPORT-FEEDBACK-JUIN-2026.md)*
- **[P2] `/api/files/[id]` coarse authorization.** Any signed-in user can fetch any non-`content-image` `StoredFile` by ObjectId (incl. patient referral PDFs, employee CVs, payout cheques). `content-image` is fully public. Security relies on consumer endpoints never leaking ids + ObjectId unguessability. The same route has a **BSON-Binary empty-body trap** (`.length` is a method on lean Binary) — keep the manual `Buffer.from(binary.buffer)` normalize branch. *(src/app/api/files/[id]/route.ts)* *(2026-09-11: kinds in `PRIVATE_STORED_FILE_KINDS` — an organization's claim form — are refused here; they have their own billing-admin route.)*
- **[P2] In-memory rate limiting.** `lib/rate-limit.ts` is a per-process map (signup, forgot-password); it does not span serverless instances and resets on cold start — weak brute-force protection. No Redis/Upstash. *(src/lib/rate-limit.ts)*
- **[P2] Field encryption is key-coupled.** AES-256-GCM via `FIELD_ENCRYPTION_KEY` with a `v1.` envelope. Misconfiguring/rotating the key makes existing ciphertext undecryptable and breaks off-session charging (`MISSING_PAYMENT_METHOD`). *(src/lib/mongoose-contact-encryption.ts)*
- **[P2] Encryption-hook ordering.** The `User` pre-save lookup-hash hook (`phoneLookupHash`) **must** run before the contact-encryption plugin (phone must still be plaintext). Reordering silently corrupts dedup. *(src/models/User.ts)*
- **[P3 — fixed 2026-08-31] `PUT /api/profile` mass assignment.** The route built its update as `{ ...body }` and passed it straight to `findOneAndUpdate`, so **any** key in the request body was written verbatim: a professional could forge `profileCompleted`, the `professionalTerms*` acceptance stamp, the server-generated `calendarFeedToken`, and most seriously `userId` — which would have re-pointed their profile at another account. Now allowlisted via `PROFILE_SELF_WRITABLE` / `pickWritable`. **The allowlist fails closed: a field added to the `Profile` schema is not self-writable until it is added there deliberately.** Keep it that way — the pricing work in spec 001 depends on it (removing `pricing` from the allowlist is what makes admin-controlled pricing enforceable rather than decorative). *(src/lib/profile-writable-fields.ts, src/app/api/profile/route.ts)*

## Money & state-machine fragility (test before touching)

- **[P1 — partly covered since 2026-09] Stripe webhook's appointment path is thinly tested.** `src/app/api/payments/webhook/route.ts` now has `route.spec.ts` (signature, idempotency claim, the premium-resource and organization-invoice branches, and pins proving the appointment path is still reached). The appointment branches themselves — receipt void/restore on refunds, guest confirmation emails, dispute flagging, `setup_intent.succeeded` — still have almost no assertions. The single most consequential payment integration point: add tests before editing those branches. *(updated 2026-09-11: the "no spec" claim was stale)*
- **[P1] LIVE Stripe webhook event subscriptions.** Receipt issuance, dispute flagging, refund reversal, and ACSS guarantee-green depend on the **LIVE** endpoint subscribing to `payment_intent.succeeded`, `setup_intent.succeeded`, `charge.dispute.created`, `charge.refund.updated`. Memory notes TEST is configured; **LIVE may be missing** the latter three. `scripts/add-stripe-webhook-events.ts` exists to help. *(deploy-coupled)* *(verified 2026-09-11, read-only: LIVE has `charge.dispute.created`, `charge.refund.updated`, `charge.refunded`, `setup_intent.succeeded` and the three `payment_intent` events; missing only `payment_intent.processing`, which bank debit for organizations needs.)*
- **[P2] `complete-session` charges money + closes billing.** `appointments/[id]/complete-session/route.ts` runs the off-session Stripe charge and finalizes billing, guarded only by an atomic `sessionCompletedAt: null` claim + Stripe idempotency key; the catch rolls back only if `!closureFinalized`. Subtle ordering — a careless edit can double-charge or strand a closure. *(has a route spec — keep it green)*
- **[P2] `cascadeAttempts` vs `refusedBy`.** The cascade counter is deliberately distinct from the never-re-propose set (release/reassign also write `refusedBy`). **Never derive cascade progress from `refusedBy.length`** — it breaks the cascade. *(src/models/Appointment.ts, src/lib/appointment-routing.ts)*
- **[P2] Matcher can overwrite an in-flight admin assignment.** Re-routing on refusal/timeout is defended only by `commitRouting`'s filter (`routingStatus: pending` + `professionalId` absent). Any new write path that ignores this filter resurrects the silent-overwrite bug. *(src/lib/appointment-routing.ts)*
- **[P2] Payout route holds real-money logic inline.** `stripe-connect/payout/route.ts`: SHA256 idempotency, atomic row-claim via `updateMany`, Connect onboarding verification, Interac-exclusion. Best-effort ledger write means a transfer can succeed while the ledger debit silently fails. *(has a route spec)*
- **[P2] No multi-document transactions.** `account-merge.ts` re-points ~15 collections then deletes the loser last (designed re-runnable, but a crash mid-merge leaves partially re-pointed data). All consistency is single-doc atomic claims. *(src/lib/account-merge.ts)*
- **[P2] Legacy UTC-midnight appointment rows.** `parseAppointmentDate` now anchors at UTC-noon, but rows written before the fix were **never backfilled** — date math on old rows can still render a day early. *(memory; no backfill run)*
- **[P3] `renderTemplate` silently drops unknown tokens.** Admin email-template typos substitute to empty (not literal); conditionals are regex with a 6-pass cap (deeply nested sections can under-render). *(src/lib/notifications.ts)*

## Testing & CI gaps

- **[P1] No CI quality gate.** `.github/workflows/deploy-whc.yml` is the only workflow and it runs **only** `next build`. **Nothing runs `vitest`, `lint`, `tsc`, or `knip` in CI**, and a green build **auto-deploys to production** — so a logically-broken-but-compiling change ships to live users. Run `pnpm test` yourself. *(verified 2026-09-09: `pnpm build` exits 0 with the lint backlog open)*
- **[P2] `pnpm lint` does not pass.** The tree has **~84 pre-existing ESLint errors** (+49 warnings), mostly `@typescript-eslint/no-explicit-any` (e.g. `admin/admins/*` routes, `lib/api-client.ts`, `lib/auth.ts`, `lib/mongodb.ts`), plus React-Compiler rules (`react-hooks/purity` on `Date.now()` in `client/dashboard/page.tsx` + `useInactivityLogout.ts`, `react-hooks/set-state-in-effect`, `react-hooks/rules-of-hooks` in `professional/dashboard/page.tsx`) and `react/no-unescaped-entities`. Because `next build` doesn't run ESLint, these don't block deploys — but it means **lint is advisory**: don't add new errors; a clean lint run is a separate, deliberate cleanup effort. *(observed: `pnpm lint` → 84 errors / 49 warnings on 2026-06-20)*
- **[P2] Large untested critical paths.** No spec for: the Stripe webhook, `payments/create-intent` / `setup-intent` / `payment-methods` / all **guest payment** routes, the 815-line `appointments/route.ts` (main booking), the 618-line `auth/signup/route.ts`, `stripe-connect/create-account`, and any cron route end-to-end. **Zero UI/component tests** (vitest runs in node env; TODO.md asked for form/integration tests that were never written). ~118 of ~154 routes have no spec. *(observed)*

## Cruft & hygiene

- **[P3] Three committed lockfiles.** `bun.lock` + `package-lock.json` + `pnpm-lock.yaml` all tracked; **pnpm is canonical**. The stale two invite wrong-installer drift. The deploy workflow installs via pnpm (pinned 10.33.2). *(repo root)*
- **[P3] Stray committed artifact.** A file literally named `c:tmpnext-dev.log` (a Windows path used as a redirect target, containing a Turbopack FATAL panic) is committed at repo root. Pure cruft — safe to delete. *(repo root, .gitignore doesn't cover it)*
- **[P3] Broken `seed` script.** `package.json` declares `"seed": "tsx src/lib/seed.ts"` but `src/lib/seed.ts` does not exist — `pnpm seed` fails. (`seed-motifs`, `reset-motifs`, `create-admin`, `seed-routing-test` are real.) *(package.json)*
- **[P3] God-files.** `lib/notifications.ts` (~6.7k lines), `email-template-registry.ts` (~3k), `app/appointment/page.tsx` (~3.1k), `MedicalProfile.tsx` (~2.2k), `signup/member` (~2.1k), `proposals` page (~1.5k). Edit surgically; don't rewrite without a plan + tests.
- **[P3] ~91 `any` + 82 `console.log`** in `src` (hotspots: `admin/admins/*` routes, `lib/api-client.ts`, `seed-routing-test.ts`, `notifications.ts`, `webhook`). 23 inline `eslint-disable` (mostly `no-img-element`).
- **[P3] Duplicate/parallel systems** (don't add a third): `Problematique` vs `ContentEntry` CMS; `PlatformSettings.emailSettings.templates` vs `EmailTemplate`; `StoredFile` vs `ClientDocument`; `guardianId` vs `accountManagerId`; `issueType` vs `needs[]`. *(src/models)*
- **[P3] Orphaned design tokens / fonts.** `src/theme.ts` (indigo/purple) and `src/config/colors.ts` don't match the live OKLCH palette in `globals.css`; `globals.css` references undefined `--font-*` vars (no `next/font` loaded → system-font fallback). Editing the wrong source does nothing visible.
- **[P3] Dead dependency.** `react-select` (+ `@types/react-select`) is installed but never imported. `pnpm prune` (knip) will flag it.
- **[P3] `Resource`/`ResourcePurchase` default to `usd`** while the platform is CAD — the marketplace appears unused/abandoned. *(src/models/Resource.ts)*

## Deploy-coupled manual steps (no automation — easy to forget)

- Stripe **LIVE** webhook must subscribe to the 4 events above.
- `scripts/backfill-post-meeting-reminder-flag.ts` must be run once, or the post-meeting collection cron can mass-mail historical clients (mitigated only by a 14-day lookback).
- Stale `jumelageSuccess` `EmailTemplate` rows (fr+en) must be deleted so they re-seed corrected (per memory).
- `MAIL_FROM` must equal `SMTP_USER` (or a verified Gmail "Send mail as" alias) or Gmail rewrites/rejects mail; `CRON_SECRET` must be set or all 5 crons 401.
- **[RESOLVED 2026-09-09] Cron coverage.** *(Was: "Vercel Hobby runs only 2 of the 5 declared crons", which is why `appointment-reminders` silently stopped sending H-72/H-48.)* Production no longer uses Vercel Cron. All **7** cron routes are scheduled **hourly** from `/etc/cron.d/jechemine` on the VPS via `run-cron.sh`, and the in-app lazy-cron (`lib/lazy-cron.ts`) remains as a belt-and-braces backstop off dashboard polls. ⚠️ Two standing rules: **never put a backup copy of the cron file in `/etc/cron.d/`** (cron runs it — see the 2026-09-09 double-firing entry below and [HANDOFF §7](../ops/HANDOFF.md)), and if you point an external pinger at a cron route, use `www.jechemine.ca`, **not** the apex — the redirect drops the auth header.
- A squash-merge to `main` sometimes does not trigger the deploy workflow — push an empty commit to re-trigger.

---

## Findings log (append below, dated)

- **2026-06-20** — Debt map created from the initial codebase audit. Items above are the baseline. Add new findings here with a date and a `[P#]` severity instead of fixing legacy code in passing.
- **2026-08-31** — **[P1, fixed]** Two disagreeing platform-fee sources. `lib/pricing.ts` computed `platformFee` from `PlatformSettings.platformFeePercentage` (db) at booking, while `complete-session` re-derived it from `process.env.PLATFORM_FEE_PERCENTAGE` (env) at charge time — so an appointment booked at 11% was billed, receipted and ledgered at 10%, and any admin-set split was silently discarded at the moment money moved. Fixed in two parts: `3dc2c24` (closure prorates the stored payout and derives `platformFee = price − professionalPayout`, so the invariant holds by construction) and the `manual-invoice` half, which now splits via `splitPriceByPlatformFee` in `@/lib/pricing` — the single source of truth, reading `PlatformSettings.platformFeePercentage`. `PLATFORM_FEE_PERCENTAGE`, `calculatePlatformFee` and `calculateProfessionalPayout` are **removed from `lib/stripe.ts`**; do not reintroduce an env-based fee, it will disagree with the database again. ⚠ Note the manual-invoice route spec previously *mocked* the helpers at a flat 20/80, so it could never have caught this — when a spec mocks the thing under test, it proves nothing.
- **2026-08-31** — **[P3, fixed]** `PUT /api/profile` mass assignment (see Security section above).
- **2026-08-31** — **[P2, fixed] Professional payment redaction was duplicated and untested.** Three hand-rolled copies of the same `delete price / listPrice / platformFee` block (in `appointments/route.ts` and `appointments/[id]/route.ts` ×2) hid the client gross and platform margin from professionals, with zero test coverage. Now one tested helper: `redactPaymentForProfessional` / `…All` in `src/lib/redact-payment.ts`. **Any new endpoint that returns an appointment to a professional must call it** — do not hand-roll a fourth copy. Accepted limitation: the client's own fiscal receipt legitimately shows the full price, so a pro shown a client receipt can still infer the margin.
- **2026-08-31** — **[P2] `calculateAppointmentPricing` has no test coverage.** `src/lib/pricing.spec.ts` covers only `formatPrice` and `getTherapyTypeLabel`; the function that decides what every client is charged and every professional is paid is untested. This is how the fee-source defect above survived. *(src/lib/pricing.ts)*
- **2026-08-31** — **[P2, partly fixed] No MongoDB backup routine on the production VPS.** cPanel backups are enabled but they back up the cPanel *account*, **not** the MongoDB dataset under `/var/lib/mongo` — so there was no database backup at all. Now nightly at 07:15 UTC via `/root/jechemine/backup-mongo.sh` (gzip archive → `/root/backups/mongo/`, `--dryRun` validation, 30-archive retention, `0600`/`0700` because archives hold PHI). ⚠ **Still open:** (a) the archives sit on the **same box they protect** — no off-site copy, so a box loss loses both; (b) **no full restore rehearsal** has been done, because the app's Mongo user is scoped to the `jechemine` database and cannot restore into a scratch copy (`not authorized`). `--dryRun` proves the archive parses, not that it restores. *(see docs/ops/HANDOFF.md §7, §9)*
- **2026-09-02** — **[P1, fixed] A doctor's referral registered the DOCTOR as the appointment's client.** Both booking routes assign `clientId` from the funnel's top-level identity, and on `bookingFor: "patient"` that identity belongs to the *referrer* — they are the one filling the form. Consequences in production: the professional's queues showed the doctor in the "Client" column with the referred patient nowhere at all; **`payment_invitation` was emailed to the DOCTOR** for the patient's session (its recipient resolves from `clientId` via `resolveAppointmentRecipient`, which has no `bookingFor: "patient"` branch); and the patient had no account row at all. Verified in prod: 3 referral appointments, `clientId` → the referrer (one of them an **admin** account), zero users matching the patient's email. Fixed at the source in `src/lib/referral-patient-account.ts` — a referral now registers (or reuses) the *patient's* account and points `clientId` at it, mirroring the loved-one flow's `data.clientId = minorUser._id`. **`referralInfo.patientEmail` is now REQUIRED** (account key + the only address that reaches the patient). ⚠ Two knock-ons to remember: (a) `resolveServiceRequestParties` must **never** fall back to the account for the referrer's contact — the account is now the patient, so that fallback prints the patient's own address as their doctor's; the routes backfill `referrerEmail`/`referrerPhone` at write time instead. (b) Legacy rows still carry the referrer as the account until `scripts/backfill-referral-patient-accounts.ts --apply` is run, so display code must keep tolerating both shapes.
- **2026-09-02** — **[P3] `resolveAppointmentRecipient` (`lib/guardian-utils.ts`) has no `bookingFor: "patient"` branch.** It routes to the loved one for `loved-one`, and to the populated `clientId` for everything else. That was correct only because `clientId` is now the patient; it means every reminder/dunning email for a referral is addressed through the account, with no referral-aware fallback. If a referral is ever allowed without a patient email again, this silently addresses the referrer. Pair any such change with a branch here. *(src/lib/guardian-utils.ts:53)*
- **2026-09-02** — **[P3] Referral data quality is unvalidated free text.** Live rows contain a phone number typed into `patientLastName` (`"Yebhe 581)397-8231"`), a clinician's own name in `referrerLicense`, and one patient email typo'd (`laouratouyehbe…` vs `laouratouyebhe…`) that will register a second, unreachable prospect. The referral form validates only presence + email shape. Consider a confirm-details step for referrals before they create accounts. *(src/app/appointment/page.tsx — `validateReferralInfo`)*
- **2026-09-02** — **[P3] A referring professional can end up with `role: "client"`.** `kaysha.constantin@santefedehealth.com` is a referring clinician sitting in `users` as a **client** (created by the pre-fix guest funnel). After the fix, referrals no longer create an account for the referrer at all — but existing rows like this remain and will show up in client lists and matching. Audit before relying on `role` to mean anything.
- **2026-09-02** — **[P1, fixed] A session dated in the FUTURE could be closed as "completed", billing a client for a session that never happened.** `complete-session` checked status (`ongoing`/`scheduled`) and the atomic closure claim, but never that the session had actually occurred. Closing is the moment money moves — it stamps `sessionCompletedAt`, issues the invoice number, creates the `ClientReceipt` and starts the dunning clock — so closing early bills immediately. Live case **JC-2026-000014**: a session booked for **9 September** was closed on **31 August**, three hours after it was created. The client was invoiced $175 and taken through the full cascade (H+12 reminder, H+36 reminder, H+48 → `overdue` + admin alert) for a session she had not attended, while the session she *had* attended (**JC-2026-000016**) was separately invoiced and paid. She reported "I already paid, why does it keep chasing me" — and she was right; there were simply two invoices, one of them bogus. Fixed by `sessionClosureWindow` (`src/lib/session-closure-window.ts`), applied **before** the atomic claim so a refused closure bills and stamps nothing. Grace window is 2h before the start — wide enough for a no-show closed early or clock skew, far too narrow for a session days away; an appointment with **no** start is deliberately left permissive so manual invoicing still works. ⚠ Note the reminder machinery itself was **correct throughout** — it dunned a genuinely unpaid invoice. Do not "fix" dunning in response to this class of complaint; check first whether the invoice should exist at all.
- **2026-09-02** — **[P3] The admin status route can still mark a future session "completed".** `PATCH /api/admin/appointments/[id]/status` sets `sessionCompletedAt = new Date()` with no date check (`src/app/api/admin/appointments/[id]/status/route.ts:42`). It is far less dangerous than the professional path — it issues no invoice and creates no receipt, so `runPaymentReminders` (which requires an `invoiceNumber`) cannot pick it up — and an admin override is arguably deliberate. Left as-is on purpose; if it ever grows invoice side effects, apply `sessionClosureWindow` there too.
- **2026-09-02** — **[P3] `ClientReceipt.status` has no "cancelled" state.** The enum is `["paid", "pending_transfer", "refunded"]`, so voiding a wrongly-issued invoice can only be expressed on the appointment (`payment.status: "cancelled"`), leaving the receipt row stranded at `pending_transfer`. Harmless today — `pending_transfer` receipts are client-hidden, and `session-post-closure` upserts by `appointmentId` so a later genuine closure reuses the row rather than duplicating it — but there is no clean way to represent "this invoice should never have existed".
- **2026-09-03** — **[P1, fixed] A saved card was invisible to the code that charges it.** A card/PAD attaches to the Stripe **customer**, but the closure auto-charge (`complete-session`) and the post-session dunning gate (`clientOwesUncollectedFee`) both read `appointment.payment.stripePaymentMethodId` — and **only the appointment-setup routes ever wrote it**. A card added from the billing page (`POST /api/payments/payment-methods`) marked the user green and left every appointment without a reference, so closure soft-skipped the charge with `MISSING_PAYMENT_METHOD`, the invoice stayed `pending`, and the client was then emailed a manual "please pay" reminder **for the card she had just saved**. Live case: `chantal.hache@hotmail.com`, green since 1 Sept 15:18 UTC, session 4 Sept — would have been dunned on closure. Fixed by `linkPaymentMethodToOpenAppointments` in `src/lib/payment-guarantee.ts`, hooked into `markClientPaymentGuaranteeGreen` (the choke point every card-save path already calls), plus `resolvePostMeetingNotification` which splits "is the fee outstanding" from "who to tell": a client holding a guarantee is never nudged, while the admin alert still fires because an uncollected fee from a client who gave us a card is **our** failure to charge. ⚠ **The invariant to preserve**: any new surface that needs a payment method must go through the appointment reference, and any new card-save path must call `markClientPaymentGuaranteeGreen` — do not attach to the Stripe customer alone. A **completed** but unpaid session is deliberately excluded from the backfill: linking there would silence its dunning without ever charging it, turning a real debt invisible.
- **2026-09-03** — **[P2, fixed] `chargeSavedPaymentMethodAfterSession` had no customer-default fallback.** It throws `MISSING_PAYMENT_METHOD` when the appointment carries no reference, rather than falling back to the Stripe customer's `invoice_settings.default_payment_method`. The backfill above closes the gap for cards saved from now on, but an appointment created by a path that never sets the reference would strand again. Fixed in `03a0a54`: closure now falls back to the customer's stored instrument via `resolveCustomerChargeablePaymentMethod` + the pure `pickChargeablePaymentMethod` (default first, then card over ACSS since a card settles synchronously; a stale default that is no longer attached is ignored). ⚠ **Two invariants**: the charge rails follow the payment method's OWN type, never the appointment's `payment.method` — an `acss_debit` charged as a card is rejected outright by Stripe; and what was charged is written back through the closure `$set`, **not** by mutating `apt` in place, because closure persists via `findByIdAndUpdate` and an in-place mutation is silently dropped. *(src/lib/stripe-off-session-charge.ts, src/lib/chargeable-payment-method.ts)*
- **2026-09-03** — **[P3] The `pending_admin` reminder exemption is conditional on `payment.method === "transfer"`.** `clientLacksPaymentGuaranteeForAppointment` only exempts a client awaiting Interac approval when the appointment's method is already `transfer`. A client who requests an Interac arrangement while their appointment still carries the default `card` method keeps receiving "register your card" reminders. Not observed to have fired wrongly in production (checked all 14 guarantee reminders ever sent — every one predated the client's card or approval), but the coupling is fragile. *(src/lib/client-payment-guarantee.ts:48)*
- **2026-09-03** — **[P3] Reminder SMS carry no location.** `sendAppointment72hSms` / `sendAppointment48hSms` take only a date label, so the office address added to the H-72/H-48 **emails** has no SMS equivalent. A client who relies on the text message still gets no address. *(src/lib/sms.ts:158,172)*
- **2026-09-03** — **[P3] Only the professional can set their office address.** `Profile.officeAddress` is in `PROFILE_SELF_WRITABLE` and edited from the pro's own profile; no admin route writes it. An admin cannot fill it in on behalf of a professional who has not, which matters because the field is new and empty for everyone — until a pro fills it in, their in-person reminders read "Adresse à confirmer avec votre professionnel". *(src/lib/profile-writable-fields.ts)*
- **2026-09-04** — **[feature] Automatic Interac reconciliation is live.** `lib/interac-reconciler.ts` reads inbound notifications from `@payments.interac.ca` (already stored as `ExternalMessage` by the on-box IMAP fetcher), matches them on `payment.interacReferenceCode`, and settles exact matches through `settleInteracPayment` — the same idempotent path as the admin's "marquer comme payé". Scheduled at `7,22,37,52 * * * *` in `/etc/cron.d/jechemine` plus the lazy-cron (10-min throttle). ⚠ **The invariants that make it safe to leave running — do not relax any of them without a very good reason:** (a) only mail genuinely FROM `@payments.interac.ca` is acted on, because a body is trivially forgeable and the sender is the only gate; (b) a transfer settles ONLY on an **exact** amount match against a named, unsettled session — partials, overpayments, missing memos, unknown codes, already-settled and cancelled sessions all go to a human with the reason recorded; (c) amounts compare in **cents**, never as floats; (d) each notification is processed at most once, stamped via `metadata.interacReconciledAt`, which is simultaneously the idempotency key and the admin audit trail. First live run: 9 examined → 1 settled (a real 150 $ payment unreconciled since 31 Aug), 7 for review, 1 not a deposit advice; the immediate second run examined 0. A prepayment on a session not yet held correctly marks the money received **without** issuing a fiscal receipt — `issueFiscalReceipt` requires a billable `sessionOutcome`, so the receipt follows at closure.
- **2026-09-04** — **[P3] Most Interac transfers arrive with no reference at all.** 7 of the 9 notifications in production carry no memo, so they can never be matched automatically — the money arrives and nothing says whose it is. The unified `INT-` code (b46feb5) is now on every payment email, so this should improve for new transfers, but clients who ignore the "message obligatoire" will always exist. The reconciler surfaces these for review rather than guessing; matching them by payer name or amount alone would be a guess about money.
- **2026-09-04** — **[gotcha] Heredoc-written files collapse `\` to `\`.** Writing a JS/TS file through a shell heredoc in this environment turns `"^\s*"` into `"^\s*"`, which in a JS *string* is just `s` — silently breaking any regex built with `new RegExp("...")`. Regex **literals** (`/^\s*/`) are unaffected. This cost a debugging cycle on the Interac parser. Prefer regex literals, or a plain line scanner (`lib/interac-field.ts`), over regexes assembled from escaped strings — and verify the written file when escapes matter.
- **2026-09-06** — **[feature] Premium (paid) resources are live on `/book#resources`.** An admin can mark a `ContentEntry` of kind `resource` premium, set a CAD price and attach a video/podcast/document link; visitors buy with a card (members or guests) and read at `/book/[slug]`. Built on the existing bilingual CMS rather than the dormant `Resource`/`ResourcePurchase` marketplace, which stays untouched and unused. ⚠ **The invariants that keep the paywall a paywall:** (a) `stripPremiumPayload` (`lib/content-premium.ts`) is the ONLY way a resource reaches a non-buyer, and it removes `contentHtml` and `mediaUrl` as **keys** — `mediaUrl` is itself the paid good for a video resource, and an empty string still renders an `<iframe src="">`; (b) `/api/content/[kind]`'s `slim` projection is an **allowlist** — never spread the DTO into it; (c) `isPremium`/`priceCents` are **mirrored** across the two locale rows, or `?locale=en` becomes a bypass; (d) the reader is `force-dynamic` because its response varies on the auth cookie and `?token=`, so any caching would serve one buyer's page to everyone; (e) prices are **integer cents** end to end — do not follow `Appointment.payment.price`'s float-dollar precedent. Money paths: the purchase intent reads the price only from the database (a body price is ignored, with a test), the webhook grant is one conditional `pending|failed → paid` update so a replayed event neither re-grants nor re-emails, and handlers never throw on a terminal condition because that would release the `StripeWebhookEvent` claim and make Stripe retry forever. A **full** refund revokes access and destroys the guest token; a **partial** refund deliberately keeps access, since the buyer retains most of the value of a fixed-price digital good.
- **2026-09-06** — **[P2] Premium video is only as private as its URL.** A paid resource embeds YouTube/Vimeo by link, so anyone who obtains the URL can watch it regardless of the paywall. The admin form warns to use an unlisted video. There is deliberately **no downloadable-file field**: `/api/files/[id]` still serves any non-`content-image` file to any signed-in user by id, so a paid PDF would be a paywall with a public back door. Do not add one until that route's auth is tightened. *(src/app/api/files/[id]/route.ts)*
- **2026-09-06** — **[P2] The guest access token is an unbounded bearer credential.** `ResourceEntitlement.accessToken` is 256 bits, stored in plaintext (mirroring `payment.paymentToken`) and deliberately **never expires** — it authorises reading a good the holder already bought outright, and expiring it would be confiscation. Compensating controls: destroyed on full refund and on dispute, `lastAccessedAt`/`accessCount` make a shared link visible, and it is scoped to one `(slug, buyer)` pair — the lookup query **must** keep `slug` in it, or one purchase would open the whole catalogue. Hashing at rest would be stronger but breaks "resend me the same link"; `accessTokenExpiry` exists unset so a future time-boxed product needs no migration.
- **2026-09-06** — **[P3] `userId` must never be written as `null` on `ResourceEntitlement`.** The uniqueness guard is a partial index on `{ slug, userId }` filtered by `userId: { $exists: true }`, and `$exists: true` **matches null** — so a single null row would collide against every other guest purchase and break buying platform-wide. Guests omit the field entirely. For the same reason `mergeResourceEntitlements` moves rows one at a time and treats E11000 as a skip: `account-merge` has no transaction, so an `updateMany` throwing partway would abort a merge that has already re-pointed half a dozen collections.
- **2026-09-06** — **[P3] The admin "Bibliothèque" nav entry is gone, but its route and models remain.** `/admin/dashboard/library` (CRUD over the dormant, monolingual, CAD-mislabelled `Resource` model) is no longer in the sidebar — two resource sections would have left the admin guessing which one sets the price visitors actually pay. The page and `models/Resource.ts` are still on disk and URL-reachable. `ResourcePurchase` has never had a row written to it and is now definitively superseded by `ResourceEntitlement`; deleting both, plus their three call sites (admin reports revenue, user-deletion cascade, account-merge), is a clean separate PR. `pnpm prune` will flag them.
- **2026-09-06** — **[verified, no action needed] The LIVE Stripe webhook already subscribes to all seven events our handlers dispatch.** Checked directly against the live endpoint `we_1UAp8NAnxgF1mcVkYxORy45X` (`https://www.jechemine.ca/api/payments/webhook`, `acct_1Sf1unAnxgF1mcVk`): `payment_intent.succeeded`, `payment_intent.payment_failed`, `payment_intent.canceled`, `charge.refunded`, `charge.dispute.created`, `charge.refund.updated`, `setup_intent.succeeded` — nothing missing, so **refund-revokes-access for premium resources will be delivered**. What was stale was `scripts/add-stripe-webhook-events.ts`, whose `NEEDED` array listed only three of them; it now lists all seven, so a future re-run or a newly created endpoint gets the full set. Re-running it is therefore optional, not a release gate. ⚠ Supersedes the older "Stripe **LIVE** webhook must subscribe to the 4 events above" line in the deploy-coupled section, which predates this check.
- **2026-09-06** — **[gotcha] The Stripe CLI can silently answer for the WRONG account.** This machine had two authorized contexts; the `default` profile was *labelled* "Santé mentale" while its session only had `acct_1TfOVnDmckWBK9zE` ("hand to hand") authorized. `stripe webhook_endpoints list -p default --live` printed the header **"Running in Santé mentale"** and returned the other business's live configuration — the only tell was the endpoint URL (a Supabase function, which Je chemine does not use). Anyone verifying production Stripe config this way would have concluded Je chemine was fine while reading someone else's account. **Always confirm the `acct_` id in the output, and the endpoint URL, before trusting a Stripe CLI result** — the display name is not proof. Fixed with `stripe reauth` (authorize the additional account) then `stripe switch context acct_1Sf1unAnxgF1mcVk --live`. Note this affects the CLI only: `scripts/add-stripe-webhook-events.ts` reads `STRIPE_SECRET_KEY` from the environment and is unaffected.
- **2026-09-06** — **[verified] The premium-resource money core was exercised against a real MongoDB**, not just against mocks. 33 checks, all passing. The ones worth recording because they could not be proven any other way: the **partial unique index on `{slug, userId}` does build** on a live server and does carry its `partialFilterExpression` (the plan listed this as unverifiable); **two different guests can buy the same resource** — i.e. the omit-`userId`-never-null rule actually holds in practice, rather than merely being asserted; a member genuinely cannot hold two `paid` rows for one slug while a `refunded` row alongside is allowed; a duplicate `stripePaymentIntentId` is rejected; the purchase route's `findOneAndUpdate` upsert has no `$setOnInsert`/`$set` path conflict (the "would create a conflict at" error class); the grant flips `pending → paid` once and reports `already-paid` on replay; an underpayment leaves the row unpaid; a full refund destroys the `accessToken` while a partial refund keeps it; `mergeResourceEntitlements` survives a real E11000 clash and still claims the guest purchase; and `previewHtml`/`mediaUrl` persist per-locale while `priceCents` stays mirrored. ⚠ **Still unexercised at the time of writing:** every HTTP route (never invoked in a running server), all Stripe API calls, the whole UI, and email rendering. Passing unit tests plus this harness is not the same as one completed purchase.
- **2026-09-06** — **[verified] Premium resources were bought, read and refunded for real.** Full end-to-end run against the dev server, a throwaway MongoDB and the **Stripe sandbox** (`acct_1Sf1utA81M9luVla`): 38 checks, all passing, and three real $19.00 CAD charges on Visa ****4242 visible in Stripe with `type: "resource_purchase"` metadata. What this proves that unit tests could not: the anonymous `/book/[slug]` HTML genuinely **does not contain** the paid body or the unlisted video URL (asserted only after confirming the real page rendered — an earlier pass was vacuous because the page was erroring); a body carrying `amount`/`priceCents`/`amountCents` is ignored and Stripe is charged the database price; a second intent call returns the *same* PaymentIntent rather than a second charge; the **webhook actually grants access** end-to-end via `stripe listen`; the buyer then sees the full body and the video player; a token for one slug does not open another; and a refund flips the row, destroys the token and **re-locks the page**. Also confirmed the guest row carries no `userId` at all. ⚠ Still unexercised: the admin CMS UI, the checkout modal in a browser, and email delivery (SMTP was off).
- **2026-09-06** — **[P3] One malformed `ContentEntry` row 500s an entire public listing.** `toDTO` (`src/lib/content-entry.ts`) calls `doc.updatedAt.toISOString()` unguarded, so a single document lacking `createdAt`/`updatedAt` throws and takes the whole `/api/content/[kind]` response with it — the `#resources` section on `/book` then renders empty for every visitor. Mongoose sets both on any normal write, so this only bites on rows inserted **outside** the models (a `mongoimport`, a migration, or a script writing to the collection directly), which is exactly how it was hit during testing. Pre-existing, left as-is per the boy-scout rule; a `?? new Date()` guard would fix it but would also hide the underlying data problem. Worth knowing before anyone bulk-imports content.
- **2026-09-06** — **[P3, FIXED 2026-09-07] `notFound()` returned HTTP 200 on every dynamic detail page ("soft 404").** Verified in production: `/medias/…`, `/nouveautes/…`, `/explore/…`, `/approaches/…` and the new `/book/…` all answer **200** for a slug that does not exist, while rendering the "Not found" body. A genuinely unrouted path (`/totally-made-up-path`) correctly returns 404, so Next's routing-level 404 works — it is `notFound()` from a `force-dynamic` page behind the Apache proxy that loses the status. Pre-existing and platform-wide, not introduced by the premium-resources work, but it means search engines index every mistyped slug as a real page. Fix once for all five routes if it ever matters for SEO.
- **2026-09-06** — **[gotcha] Every page's HTML contains ALL translated strings**, because next-intl ships the whole message bundle to the client. So `html.includes("Débloquez cette ressource")` is true on a **404 page**, and any test that asserts "this page rendered" or "this text is absent" using a *translated* string is meaningless. Assert on values that exist only in the database — a title, a slug, a marker inserted by the test — or on `<title>`, which `generateMetadata` controls. This nearly produced a false negative while verifying that a resource had been unpublished; the real proof was `<title>Not found</title>`, which only appears when the published document is absent.
- **2026-09-07** — **[P3, fixed] The soft-404 was caused by a root `loading.tsx`.** `src/app/loading.tsx` put a Suspense boundary above **every** route, so Next streamed a shell — and flushed HTTP **200** — before any page component ran. `notFound()` could then only swap the rendered UI; the status was already on the wire. That is why an unrouted path (`/totally-made-up-path`) 404'd correctly while `notFound()` from `/book/…`, `/medias/…`, `/nouveautes/…`, `/explore/…` and `/approaches/…` did not. Reproduced locally with `next start`, so it was never the Apache proxy. Fixed by moving the file to `src/app/(privilaged)/loading.tsx`: the dashboards keep their spinner, and the public SEO-facing tree no longer streams, so all five detail routes now return a real 404. ⚠ **Do not add a `loading.tsx` (or any Suspense boundary) above the public routes again** — it silently reintroduces soft-404s across the whole site, with no error and no test failure. The 404 page itself was also rewritten: it was hardcoded English ("Are you Lost? go Home") on a French-first platform, which now matters because crawlers and visitors genuinely land on it.
- **2026-09-07** — **[feature] SEO groundwork before the first Google crawl.** The site was reachable on **four addresses at once** — `jechemine.ca` and `www.jechemine.ca`, each over http and https — with **no redirect and no canonical tag**, so search engines could have treated them as duplicate sites and split the ranking; worse, anything indexed under the bare domain would not have counted toward the Search Console property, which is registered as `https://www.jechemine.ca`. There was also **no `robots.txt`, no `sitemap.xml`**, and almost every public page inherited the same site-wide `<title>`. Now: `src/middleware.ts` 308s the apex to the canonical host, the root layout emits a self-referencing canonical on every route, `src/app/robots.ts` and `src/app/sitemap.ts` are generated (the sitemap **dynamically from the CMS**, 44 URLs), and ten public pages have their own title + description under a new `Seo` i18n namespace. ⚠ **The sitemap is not optional here**: the listings on `/book` are fetched client-side, so the server HTML contains **no links to the detail pages at all** — a crawler following links would never find the ~28 content pages. If those grids are ever made server-rendered, the sitemap still matters, but that is the reason it exists today.
- **2026-09-07** — **[gotcha, near-miss] A scheme-based redirect in middleware loops forever behind this proxy.** The first version of the host canonicalization also sent http → https, guarded on `x-forwarded-proto === "http"`. Local testing showed it firing on `Host: www.jechemine.ca` with no proxy header set at all, because Next fills that header in itself. ⚠ **Correction after inspecting the live server:** the Apache vhost sets `RequestHeader set X-Forwarded-Proto "https"` on the **port 80 vhost as well**, so in production the app is told every request is https and the rule would never have fired at all — no loop, but no redirect either. Either way the app layer cannot see the real scheme, which is why this belongs in Apache. The shipped version redirects on the **host only**, and only for hosts in an explicit allow-list, so the target can never match again. HSTS (already sent, with preload) covers http for browsers; a proper http → https rule belongs in the Apache vhost, not here. ⚠ Never key a middleware redirect on the request scheme in this deployment.
- **2026-09-07** — **[P3, fixed] The "default" social links were guesses, and they were sitting in the database.** `DEFAULT_SOCIAL_LINKS` hardcoded `facebook.com/jechemine`, `x.com/jechemine`, `instagram.com/jechemine` and `linkedin.com/company/jechemine`. Every one was wrong: the real accounts are `facebook.com/CAJTerrebonne` and `linkedin.com/company/je-chemine`, **`x.com/jechemine` is a 404**, and `instagram.com/jechemine` is an unrelated account with 0 followers and no display name. **The trap was that these were not render-time fallbacks — they were stored data.** The admin settings form round-trips whatever `GET /api/admin/settings` returns, so the first time an admin saved anything, all six guessed values were persisted into `platformsettings.socialLinks`; production held `x` and `instagram` as literal placeholder strings. A code-only fix would have changed nothing on the live site. **Ordering mattered:** the Organization JSON-LD omitted the dead X link *precisely because it equalled the default*, so emptying the defaults first would have stopped that filter matching and published `x.com/jechemine` into `sameAs` — the data had to be cleaned **before** the code changed. Fixed in that order: the two placeholders were set to `""` on prod (guarded on the exact expected value; **set to `""`, never `$unset`** — `getSocialLinks` fell back to the code default only when the field was *absent*, so unsetting restores the guess), then `DEFAULT_SOCIAL_LINKS` was emptied, `getSocialLinks` was narrowed to return only values that parse as an http(s) `URL` (it also crashed on a non-string legacy value, since `.lean()` makes the declared `string` type a lie), and the JSON-LD default-comparison workaround was deleted. `src/lib/platform-contact.spec.ts` now fails if any default is ever a non-empty URL. **Same-day update:** the owner confirmed the Instagram account *is* theirs, and `socialLinks.instagram` was restored to `https://instagram.com/jechemine`; it renders in the footer and in `sameAs` again **with no deploy**, which is exactly why the placeholder scrub was kept out of the read path (a read-time filter would have silently ignored an admin who deliberately re-entered that URL). Only `x` stays empty, because that account does not exist — if one is ever created, setting it in Admin → Settings feeds both surfaces automatically.
- **2026-09-07** — **[P3] A content slug can never be renamed, and the form pretends otherwise.** The edit page passes `slugEditable={!wasPublished}`, so the slug input *unlocks* as soon as an entry is set back to draft — but `handleSave` never puts `slug` in the PUT body, and `PUT /api/admin/content/[kind]/[slug]` writes the slug straight from the URL params. Typing a new slug therefore does nothing at all: the field accepts it, the save reports success, and the entry keeps its old URL. Found through `/medias/httpstelequebectvregarderetre-ado12` — an admin pasted a Télé-Québec URL into the title field and the client-side slugifier turned it into the permanent public URL, complete with a duplicate `<title>` and no meta description. **Workaround: delete the entry and recreate it**, which is safe for the editorial kinds. It is *not* available for `kind: "resource"`: the DELETE guard refuses while any `status: "paid"` entitlement exists (correct — people bought that URL), so a mis-slugged premium resource cannot be fixed at all today. A real fix sends `slug` in the PUT body and renames both locale docs under a uniqueness check; the cheap fix is to stop unlocking the input in edit mode so it no longer lies.
- **2026-09-07** — **[P3, fixed] Every shared article link previewed as the site name.** The root layout sets `openGraph.title` and `twitter.title`, and Next resolves metadata field-by-field up the tree, so the four content detail routes (`/explore`, `/approaches`, `/medias`, `/nouveautes`) — which set only `title` and `description` — shipped "Je chemine - Soins en santé mentale" to every Facebook/LinkedIn/X preview. `/book/[slug]` had hand-rolled `openGraph` and so looked correct, but had **still** inherited the generic `twitter:title`, which is the drift that made a shared helper worth it. `src/lib/content-metadata.ts` now returns both blocks and all five routes spread it; `content-metadata.spec.ts` pins that Twitter is set independently. The failure is silent — the page renders fine and the `<title>` tag is right — so only a link preview or that spec catches a regression.
- **2026-09-07** — **[decision] "Link-only" media entries were considered and deliberately not built.** `/medias` cards always route to `/medias/[slug]`, and `MediaType` is `"article" | "video" | "podcast"` — there is no `"link"` value, so an entry that exists only to point at outside coverage still gets a full detail page. Making one route straight out means a new type or flag threaded through the **18 files** that reference `mediaType`, plus FR/EN i18n lockstep, a schema enum change on a live collection, admin UI, the sitemap and the detail page. Declined for now, on the owner's call: a page on our own domain about an external segment, carrying our own commentary, keeps the visitor and is indexable, whereas a bare outbound card hands the traffic away and gives Google nothing — and the site is in the middle of its first indexing push. **Revisit when there is a real press-mentions list** (roughly ten-plus "we were featured in X" entries), where stub pages would become thin-content noise; today exactly one entry is outbound and its three siblings are self-contained articles. Until then the rule is: an outbound media entry gets its own title, summary and a paragraph of our own words, or it should not be published at all.
- **2026-09-09** — **[P2, fixed] "On ne reçoit pas les emails de nouvelles demandes" was a mailbox problem — but it exposed two silent-failure modes in the alert path.** The reported symptom was that the team never got the new-demande alerts. The code was working: `journalctl` showed **27 alerts to `support@jechemine.ca` in 30 days**, 3 on the day it was reported, every one accepted by `mailpro5.whc.ca` with a message ID; `adminAlertEmail` was set, email was enabled globally, and SPF/DKIM/DMARC all resolve (`p=none`, so nothing is being rejected by policy). `MAIL_FROM` and the alert recipient are **both** `support@jechemine.ca` — the mail was arriving in a mailbox nobody was watching. ⚠️ **Diagnosis gotcha, and the fastest tool for "did it actually send?":** `journalctl -u jechemine.service --since "30 days ago" | grep "Email sent"` prints `Email sent [type] via smtp to <addr>` for every send. Two traps when reading it — the admin alert and the *client* confirmation shared one type, so **only the recipient told them apart** (fixed below), and `sendEmail` **returns `true` when the transport is unconfigured**, logging "would have sent" — an unconfigured relay reports success to its caller. Two defects fixed in the same pass: **(1)** the admin alert was tagged `service_request_onboarding`, the CLIENT's confirmation type, which Admin → Settings labels *"Courriel de bienvenue (formulaire de demande)"* and describes as sent to a client — switching that off to stop a client email would have silently killed the team's own alerts. It now has its own `admin_new_service_request` type, toggle and admin label. **(2)** `sendAdminNewServiceRequestAlert` returned **silently** when no recipient was configured; it now warns, so a cleared `adminAlertEmail` leaves a trace instead of swallowing every demande. `src/lib/notifications-admin-service-request.spec.ts` pins both (mutation-verified: 5 of its 6 cases fail against the old code). **Follow-up, now done:** the misleading `after()`-is-a-Vercel-workaround comments at both call sites, and every other stale Vercel claim in the repo, were corrected the same day — see the entry below. `after()` does work on the self-hosted server (the client confirmation email uses it and sends fine); the comments cost real time during this diagnosis by suggesting a serverless teardown that cannot happen here.
- **2026-09-09** — **[P2, fixed] Every scheduled job was running twice, because `/etc/cron.d/` held two backup copies of the cron file.** `jechemine`, `jechemine.bak.1785763065` and `jechemine.bak.1788531196` all sat in `/etc/cron.d/`, and **cron ran all three** — a dot in the filename does **not** disable it on this box. Result: every reminder job fired **twice** (three times 09:00–14:00 UTC, where the older backup's daily schedule overlapped the hourly one), and the app watchdog and inbound-mail sync ran **3×**. The active file's own header warns "so they do NOT double-run and double-email clients", which the copies silently defeated. Client-facing damage was limited only by the runners being idempotent — that was luck, not design. Fixed by moving both copies to `/root/jechemine/cron-backups/`, outside any directory cron reads; verified afterwards that each job fires exactly once. Detection recipe and the standing rule are in [HANDOFF §7](../ops/HANDOFF.md).
- **2026-09-09** — **[docs, fixed] The repo claimed production ran on Vercel; it has not for months.** `AGENTS.md` called `pnpm build` "the deploy gate (Vercel runs this)" and stated "there is **no CI gate at all**"; the architecture, product and PRD docs described `vercel.json` crons, Vercel Cron and "MongoDB Atlas"; `next.config.ts` documented TLS as "terminated by the host (e.g. Vercel)"; and both appointment routes explained `after()` as a **Vercel serverless keep-alive**. Production is a **WHC VPS** running `next start` under systemd behind Apache, deployed by `.github/workflows/deploy-whc.yml`, with crons in `/etc/cron.d/jechemine` and MongoDB on-box. The stale `after()` comments actively misled the 2026-09-09 email investigation — they suggested a serverless teardown that cannot happen on a long-lived Node server. All corrected, and the vestigial `vercel.json` (reduced to a bare `$schema`, its `crons[]` already removed) is deleted. **Verified**: `cheminement-b77i.vercel.app` returns **404** and `www.jechemine.ca` resolves to the VPS. The `cheminement-b77i` project is **deleted** (owner-confirmed same day), so its stored env vars went with it and the Vercel MCP config was removed too (`.mcp.json` and `.vscode/mcp.json` deleted — Vercel was their only server — plus 7 entries out of `.claude/settings.json`). ⚠️ **Correction to an earlier draft of this entry:** it listed `FIELD_ENCRYPTION_KEY` among secrets to "rotate". That was wrong and would have been destructive — `User.phone` and `User.location` are encrypted with it, so replacing it without a decrypt-then-re-encrypt migration makes existing rows permanently unreadable. It is key material, not a credential.
- **2026-09-11** — **[P2, fixed] The "Aucun paiement — séance passée" admin alert repeated every hour for the same session.** Reported by the client as "le système me fait plusieurs rappels pour la même situation". The Réception panel (the support@ inbox synced into `externalmessages`) held **18 identical copies for one client in 18 hours**, one per run of `payment-guarantee-reminders` at `:30`. Cause: for a client with a card on file (`paymentGuaranteeStatus: "green"`), `postMeetingPaymentReminderSent` is never set — by design, so an uncollected fee keeps surfacing to the admin until reconciled — but nothing limited how *often* it surfaced. That "pressure" was tolerable when the job ran daily on Vercel; it became hourly when crons moved to `/etc/cron.d/jechemine`, and the 14-day lookback allowed ~336 copies per session. **Fixed:** the alert still repeats until the fee is reconciled, but at most once a day per session — `Appointment.postMeetingAdminAlertSentAt` + `isPostMeetingAdminAlertDue()` (10-min jitter tolerance so cron timing can't slip it an hour a day), and `sendAdminNoPaymentBeforeMeetingAlert` now returns whether a copy was accepted, so the clock only starts on a real send and an SMTP failure retries next hour. `payment-guarantee-reminders.spec.ts` replays the incident (18 hourly runs → 1 alert); mutation-checked — with the limit removed it fails with exactly "got 18 times". The one session re-alerting in production was stamped just before deploy so the client didn't receive "one more" on the first new run. ⚠️ **General lesson: moving a job from daily to hourly silently multiplies every alert that relied on the cadence for dedup.** When changing a cron schedule, audit what the runner sends per pass, not just whether it runs. The other repeating subject in that inbox, *"Liste générale — refus en cascade"*, showed same-minute pairs on 2026-09-09 — that was the duplicate-cron-file incident (entry above), already fixed; its remaining repeats are distinct cascade events. **Not changed, still open in this same alert:** **(1)** its hardcoded fallback body says the client *"n'avait aucun mode de paiement configuré"* and that *"une relance a été envoyée automatiquement au client"* — but the case that repeats is precisely a client who **has** a card and to whom **no** reminder was sent (it is our failure to charge, per `resolvePostMeetingNotification`), so the text misdirects the admin; **(2)** it is sent under the `admin_interac_trust_request` email type, so disabling the Interac-trust alert in Admin → Settings would silently kill it too — the same type-sharing trap fixed for the new-demande alert on 2026-09-09.
- **2026-09-11** — **[P1, fixed] Clients could write billing fields, and anyone signed in could delete any appointment.** Found while planning third-party billing (spec 002), verified in code before fixing. **(1)** `DELETE /api/appointments/[id]` checked only that a session existed: any signed-in user — a client included — could hard-delete any appointment by id, including closed, invoiced, ledger-credited sessions. No screen called it. Now admin-only, and 409 `APPOINTMENT_HAS_BILLING_HISTORY` when `sessionCompletedAt`, `invoiceNumber`, `fiscalReceiptIssuedAt` or a moved-money payment status is present — the guard sits in the `findOneAndDelete` filter, so it is atomic. **(2)** `POST /api/appointments` passed the whole body to `new Appointment(data)` — a client could book with `payment: { status: "paid" }`, closure skipped the charge as settled, and an official receipt was emailed for an unpaid session. **(3)** `POST /api/appointments/guest` spread the body into root fields (`invoiceNumber`, `sessionCompletedAt`, …). **(4)** `PATCH /api/appointments/[id]` wrote the body unfiltered, operator keys included (`{ "$set": { "payment.status": "paid" } }`); an authorized client could also mark their own session completed or overwrite the professional's session notes. All three now go through allow-lists in `src/lib/appointment-writable-fields.ts`, built from an inventory of every caller; dropped fields are logged. ⚠️ **Rule going forward: a new Appointment field is NOT writable through these routes until it is added to an allow-list on purpose — keep it that way; never switch them back to a deny-list.** **Still open, found during this work, not changed:** the professional's session-status dialog (`professional/dashboard/sessions/[id]/page.tsx`) offers `completed` and `no-show`, which it sends through the generic PATCH — that marks a session done **without** going through `complete-session`, so no closure, no charge, no invoice, no ledger credit. Professionals are allowed to do this today, so it was left as-is; it should either route to `complete-session` or drop those two options. The unused client helper `appointmentsAPI.delete` in `src/lib/api-client.ts` can be removed.
- **2026-09-11** — **[P2, fixed] Two `/pay` bugs on fees the client genuinely owes.** **(1)** Late-cancellation and no-show fees could not be paid through the link closure emails for them: those sessions carry status `cancelled` / `no-show`, and `/api/payments/guest` refused both. Payability is now decided by `decideGuestPayment()` in `src/lib/guest-payment-eligibility.ts` — a closed billable outcome (`sessionCompletedAt` + `cancelled_late`/`no_show`) is payable; free cancellations stay refused. **(2)** Creating a payment intent set `payment.status = "processing"` before anyone paid; every reminder treats `processing` as settled, so an abandoned form silenced dunning for good. POST now only records the intent (and reuses one still waiting for the same amount and method); the new `POST /api/payments/guest/confirm`, called by `/pay` after `stripe.confirmPayment`, sets `processing` only when Stripe reports it in flight (a bank debit). A row already stuck at `processing` heals on the next attempt if Stripe shows nothing in flight; a Stripe outage resets nothing. Production had **0** stuck rows at the time, so no repair ran. ⚠️ The webhook still does not subscribe to `payment_intent.processing`; the confirm call is the only signal for a bank debit in flight. If a payer closes the tab in the instant between confirming a debit and the confirm call, the session stays `pending` and gets reminders while the debit clears — annoying, not lossy; the `succeeded` webhook settles it.
- **2026-09-11** — **[refactor] One `SETTLED_PAYMENT_STATUSES`.** It existed in three copies (`client-payment-guarantee.ts`, `payment-guarantee.ts`, `interac-reconciliation.ts`); the latter two now import it, and `isSettledPaymentStatus()` is exported. Add any new "client owes nothing" status there — every dunning gate reads it. `src/types/api.ts` `PaymentStatus` caught up with the model enum (`partially_refunded`, `overdue`).
- **2026-09-11** — **[feature, dark] Spec 002 phase 2 — admins can choose or change who pays; "covered" is handled everywhere money is shown.** `PUT /api/admin/appointments/[id]/billing-override` sets the payer before closure (409 once closed); `POST /api/admin/appointments/[id]/payer` changes it after, through `src/lib/session-payer-reassign.ts`, which recomputes with the same resolver as closure. Both require an active admin with `manageBilling` (`requireBillingAdmin`, `src/lib/organization-admin.ts`). ⚠️ **Invariants of the reassign — keep them:** refuse once the client paid/processing/refunded (refund first) or the session is on an organization invoice (void first); the write is conditional on the snapshot's `plannedAt`, so a double click applies once; a change in what the professional is owed is a NEW ledger row with `adjustsAppointmentId` (never an edit — the session's own credit is unique per `appointmentId`); a cap slot is taken or given back exactly once; the client is emailed only when their side changed, and an open card payment for the old amount is cancelled. Also: professionals now see their pay for the whole session (`redactPaymentForProfessional` reads `thirdPartyBilling.proPayoutTotalCents`), revenue reports sum client + organization (`SESSION_BILLED_TOTAL_EXPR`, `src/lib/billing-totals.ts`), the sales journal holds organization credits until the organization pays (`isLedgerCreditCleared`), and "mark paid" / "resend payment" refuse a covered session (409 `NOTHING_OWED`). **Known gaps, left on purpose:** (1) moving a closed session to the client sends them the payment link; it does not charge their saved card — closure's off-session charge is not reused. (2) A client invoice number (`JC-…`) allocated at closure stays on a session later moved to the organization; nothing is owed on it. (3) The admin "manual receipt" button is still offered on covered sessions and would print a 0 $ receipt. (4) Stripe Connect payouts only move the client-card share; an organization's share reaches the professional through the ledger payout. (5) The pro PATCH response in `appointments/[id]` does not select the payer snapshot, so a covered session briefly shows the client share until the page refetches. (6) Pre-existing, not changed: the admin reprice route does not cancel an open payment intent for the old amount (the reassign does), `resend-payment` still accepts already-paid sessions, and the pro schedule panel shows `payment.price`, which is redacted for professionals (always "0.00").
- **2026-09-11** — **[feature, dark] Spec 002 phase 3 — organizations, coverages, the booking declaration, and the switch.** Admin → "Organismes payeurs" lists the paying organizations and carries the `organizationBillingEnabled` switch (confirmation both ways). The patient file gets a coverage panel (terms, cap, validity, consent recorded/withdrawn with a mandatory note into an append-only `consentLog`) and a payer control per appointment. While the switch is on, the booking funnel's review step offers "someone else pays" (`ThirdPartyPayerFields`); the team's new-request alert names the declared payer, and the declaration waits in the patient file to be confirmed (→ coverage carrying the client's booking consent) or refused. Professionals see "PAE · 3/6" (`coverageBadgesFor`) — the kind of payer and sessions used, never the organization's name or amounts. Clients get "coverage confirmed", "one covered session left" and "used up" emails; the team gets "one left". ⚠️ **Invariants — keep them:** the stored `payerDeclaration` is built server-side from `thirdPartyPayer` (`buildPayerDeclaration`) and needs the consent box — a raw `payerDeclaration` in the body is dropped; a declaration is reviewed once (conditional on `status: "pending"`), and its held sessions are settled with it (organization on confirm, client on refusal); every coverage notice is claimed on the coverage before sending and given back if nothing was sent; one active coverage per person (partial unique index, verified on a real MongoDB); an organization that still covers someone cannot be archived. **Not done in this phase:** no "declared payer" badge in the requests queue (the team alert and the patient file carry it); the badge counts sessions used, not "this is session 4 of 6". **Found while building, not changed:** (1) admin email detail values are not HTML-escaped (`createDetailRow` in `notifications.ts`) — client names and motifs go in raw; the new payer fields are escaped. (2) The "Liste générale — refus en cascade" admin alert is sent under `service_request_onboarding`, the client welcome email's type — switching the welcome email off silences it, the same bug class as the 2026-09-09 new-request incident. (3) `Auth.memberSignup.reviewPhone` exists in `fr.json` only, so English visitors see the raw key.
- **2026-09-11** — **[feature, dark] Spec 002 phase 4 — invoices and statements to organizations (the first time patient data leaves).** Admin → "Factures aux organismes": what each organization owes and has not been invoiced, drafts (one per session, or a monthly statement), send, resend, void, payments received. `/api/cron/organization-billing` (hourly, `50 * * * *`) drafts last month's statements (Montréal month), per-session drafts, the organization's opt-in auto-send, one review email to the team, and flips overdue invoices — and does nothing while the switch is off. ⚠️ **Invariants — keep them:** invoice lines are built only by `buildInvoiceLine` (field by field: patient, case number, professional name/title/licence, date, duration, amount) and the model's line schema is `strict: "throw"`; the PDF's input type has no clinical field; **nothing is sent while any line's coverage lacks consent** (`blockedLines`), with no override, checked again on every resend; a send checks for stale lines, claims `draft → issuing`, reserves the sessions (one live invoice per session) and only then takes a `JCO-` number from its own counter; void releases the sessions, keeps the number and renames the `draftKey` so the period can be drafted again; an invoice with a payment cannot be voided; payments never exceed the balance and are idempotent per `externalRef`; paid in full marks the sessions `orgStatus: paid`, which the sales journal waits for; the org email body names no patient; every disclosure is in `sendLog`. **Not in this phase:** online payment and reminders to organizations (phase 5 — "overdue" is only a status for now; a partly paid invoice past due stays "partially_paid"); refunds to organizations (`refunded` is unused); the admin-only attachment for organizations that require their own form; the manual invoice's `payer` field. Drafts are rebuilt every run until sent — the send's stale check protects what an admin reviewed. **Found, not changed:** the header comment of `/etc/cron.d/jechemine` on the VPS still says the jobs are "DISABLED until this box is PRODUCTION" and mentions Vercel; they are live.
- **2026-09-11** — **[feature, dark] Spec 002 phase 5 — getting paid by an organization: pay link, card by webhook, Interac matching, reminders.** Every issued invoice gets a pay token (`payToken`, 256 bits) and its emails a **pay link** `/org-pay?token=…`: the page and `GET /api/organization-invoices/pay` show the organization, the number, the amounts and the due date — **the route never reads the invoice lines** (patient names). Card only (`src/lib/organization-invoice-card.ts`): the amount is the balance read from the database; the PaymentIntent carries `type: "organization_invoice"` + the invoice id and **no `appointmentId`**; an open intent for the same amount is reused, one for an old amount is cancelled; voiding the invoice or recording a payment by hand cancels an unfinished one. The Stripe webhook settles it in a branch **before** the `appointmentId` bail (as for premium resources); refunds, failed refunds and chargebacks on an organization's card payment are looked up **before** the appointment lookup. Interac: an organization writes the **invoice number** (`JCO-…`) as the transfer message — the reference the PDF already asked for, so there is one reference per invoice (the plan's separate `ORG-` code was dropped; `interacReferenceCode` is unused). `REFERENCE_CODE_RE` accepts `INT-…|JCO-…`; the reconciler settles a `JCO-` transfer only on the exact balance, keyed on Interac's transfer reference. Reminders (`src/lib/organization-dunning.ts`, from the hourly organization-billing run): at the due date and 14 days later, weekdays 8 h – 18 h Montréal, number and balance only (no patient, no PDF), then one team email at 30 days; every step is stamped before it is sent and given back if nothing went out; disputed invoices are not chased. Four new email types: `organization_payment_reminder`, `organization_payment_received`, `admin_organization_invoice_overdue`, `admin_organization_payment_review`; the organization emails (and phase 4's `organization_invoice` / `organization_statement`, which were missing) reply to the payment inbox. ⚠️ **Invariants — keep them:** all money in goes through `src/lib/organization-invoice-settlement.ts`; money already in the account (card, Interac) is **always recorded, exactly once per `externalRef`**, even on a void or already-paid invoice or beyond the balance — then written to `paymentEvents` and emailed to the team; **nothing is ever refunded automatically**; an admin still cannot record more than the balance or pay an invoice not awaiting payment; the status follows the balance through conditional writes (`syncInvoiceStatus`) and the sessions follow the status (paid ↔ invoiced); a refund only ever grows a card payment's `refundedCents` (`$max`) unless Stripe's charge says otherwise (a failed refund, read from Stripe), and paid/balance are recomputed from the payments in one pipeline write; the pay page never gains a patient field. **Not in this phase:** a bank debit (PAD) option for organizations; refunding an organization from the admin screen (done in the Stripe dashboard; the webhook records it); cancelling an open card payment when an *Interac* transfer settles the invoice (the reconciler has no Stripe client — a second payment from a stale tab lands as an overpayment for review); phase 6 reporting. ⚠️ **Deploy-coupled:** refunds and chargebacks on organization payments depend on the LIVE webhook subscribing to `charge.refunded`, `charge.dispute.created` and `charge.refund.updated` — see the P1 above; card payments themselves only need `payment_intent.succeeded`.
- **2026-09-11** — **[feature] Spec 002 phase 6 — receivables, anomalies, the payer, and the client's covered sessions.** Admin → "Factures aux organismes" opens on **Comptes à recevoir et anomalies** (`GET /api/admin/organization-invoices/receivables`, billing admins only, `?format=csv` for the accountant): open balances per organization by age (not yet due / 1–30 / 31–60 / 61–90 / 90+ days, most overdue first; a credit is not a receivable) and six anomaly lists — overdue invoices, sessions waiting for a payer decision, organization sessions still not invoiced 35 days after closure, coverage counters that disagree with the sessions actually paid (over the cap, used up below it, still active at it, or a counted session that no longer holds its slot — e.g. reassigned to the client), negative margins over the last 90 days, and payments to review (credit, dispute, refund, money on a void invoice). Comptabilité → Anomalies points there (that page is open to every admin; organization data stays behind `manageBilling`). Admin → Facturation shows a **Payeur** line per session: client, the organization with its invoice status and `JCO-` number, the payer outside the platform, or "À décider" — organization names and invoice numbers from two batched lookups per page. The client's billing page shows **« Séances prises en charge par X — 3/10 séances utilisées »** (`GET /api/client/coverages`): own coverages and loved ones', named as the client typed them. ⚠️ **Invariants — keep them:** the client endpoint returns **nothing while the organization-billing switch is off** (closure then charges the client, so "covered" would be false) and never the rate, amounts, case number or consent record; the receivables lists carry client names, dates and amounts only — no motif, act or note. **Not in this phase:** coverage for a guardian viewing a *managed account* (`?accountId=`) — the card shows the signed-in user's own coverages only; the anomalies are read-only lists (acting on one happens in the client's file or the invoice list); no email digest of anomalies (the phase 5 alerts cover what needs acting on at once). **Found, not changed:** the admin billing and accounting routes check only `role === "admin"`, not a billing permission, while the organization routes require `manageBilling` — the payer line therefore shows organization names to any admin; the older admin screens were already showing client names and amounts to every admin.
- **2026-09-11** — **[feature + fix] Create the organization a client declared, from the client's file.** Confirming a booking declaration no longer means leaving for "Organismes payeurs" and coming back: when the declared name is not on file, the confirm dialog says so, suggests look-alikes (`src/lib/organization-name-match.ts` — case, accents, punctuation and "inc./ltée" ignored; "PAE Desjardins inc." ≈ "PAE Desjardins Montréal"), and offers "Créer « … »", which opens the same organization form (now the shared `OrganizationEditorDialog`, also used by "Organismes payeurs") prefilled with that name and selects the result. The preselection uses the same normalized match instead of an exact lower-case comparison. **Fixed:** "Confirmer…" was clickable before the organization list had loaded — the declared name was then matched against an empty list, shown as "not on file", and an admin could create a duplicate (the API only refuses an exact duplicate name). The buttons now wait for the list. Also, the first declaration could not be confirmed at all while no organization existed (the button was disabled). ⚠️ **Keep:** look-alikes are suggestions only — never merge or pick one automatically; the server still refuses an active organization with the exact same name.
- **2026-09-11** — **[fix] An Interac client's session stored as "card" was closed as a card with no card — and the billing screen said « Carte validée ».** JC-2026-000014 (Élorie, 9 Sept, closed 10 Sept): the client never gave a card (no Stripe customer, no setup, no charge attempt — checked in live Stripe); an admin had approved her Interac arrangement (`interac_trust`, preference Interac). Her professional booked the session from the pro portal, and `payment.method` defaults to **"card"** — `/api/professional/appointments`, `/api/admin/appointments`, `/api/appointments/request-with-current-pro` never set it, and the follow-up created at closure copies it. Closure looked for a card, found no customer, left the invoice pending and told the professional to ask for a card; the client was correctly sent the invoice (card link **and** Interac reference), but the admin billing badge printed « Carte validée » for **every** "card" session without checking anything, so the unpaid invoice read as a failed charge. **Fixed:** closure (`complete-session`) bills a client who pays by Interac (`paysByInterac`: `interac_trust` or preference Interac) **by Interac when there is nothing to charge** — method `transfer`, Interac reference kept or issued, due date +24 h, ledger channel `transfer`, no card warning; a card on file is still charged, a declined card is still a card problem, a card client with nothing on file is unchanged. The billing badge now comes from `paymentAssurance()` (server-side; the card reference never reaches the browser): « Carte validée » only with a card linked, a green Stripe guarantee, or a card charge that went through; « Aucune carte » / « Aucune carte — Interac » otherwise; « Dossier validé » only for an approved Interac arrangement, « Entente à valider » while pending. **Found, not changed:** (1) the booking paths above still store "card" for Interac clients — so Interac clients awaiting approval (`pending_admin`) get the card-guarantee nudges (`clientLacksPaymentGuaranteeForAppointment` only spares `pending_admin` + `transfer`). Not changed at booking because a "transfer" session is never auto-charged even if the client saves a card later (`linkPaymentMethodToOpenAppointments` links the card but does not change the method), and flipping it there could charge a client whose Interac transfer is still awaiting review; closure now decides from the client instead. (2) `settleInteracPayment` (Interac reconciler + both admin "marquer comme payé" buttons) only sets `transfer` when the method is empty, so a "card" session settled by Interac keeps "card": its receipt names the card and the Connect auto-payout filter (card/PAD only, H9) would accept it — now rare for Interac clients, still possible for a card client who pays by Interac. (3) **Reopening a closed session by hand must also delete its ledger credit** (unique per appointment): the 2 Sept manual restore of this session left the 31 Aug credit, so the real closure's credit was silently dropped (duplicate key ignored) and the stale one kept the August cycle and the "stripe" channel — which the sales journal counts as collected.
- **2026-09-11** — **[fix] Sessions carry the client's way of paying; money confirmed outside Stripe is recorded as Interac.** Closes (1) and (2) of the entry above. **New sessions** (`paymentMethodForNewAppointment`, used by `/api/professional/appointments`, `/api/admin/appointments`, `/api/appointments/request-with-current-pro` and the follow-up created at closure): an Interac client — approved or awaiting approval — with no card on file gets `transfer`; a client with a card on file never gets a new Interac session, not even as the follow-up of one (closure would bill it by Interac and leave the card uncharged); everyone else as before (`card`, or the copied method). **No card nudges while an Interac request awaits approval:** `clientLacksPaymentGuaranteeForAppointment` spares a `pending_admin` client who pays by Interac on every session, not only those labelled `transfer`; the admin dashboard flag now loads `preferredPaymentMethod` for the same rule. **A card saved later:** `linkPaymentMethodToOpenAppointments` also puts the client's open, unpaid sessions on the saved instrument's rails (`card` / `direct_debit` — a PAD charged as a card is refused by Stripe), Interac ones included, **except a session with an Interac reference** (instructions went out; money may be on its way), which stays Interac with the card linked. **Settlement:** `settleInteracPayment` (Interac reconciler + both admin "marquer comme payé" buttons) records `transfer` unless the session is `manual` or a PAD debit is already `processing` on Stripe, and moves its ledger credit from `stripe` to `transfer` — the official receipt reads « Virement Interac » and the Connect auto-payout (card/PAD only) no longer accepts it. ⚠️ **Keep:** never move a session that has an Interac reference to a card automatically; never label money confirmed outside Stripe as card. **Not changed:** upcoming sessions stored as "card" before this fix keep their label (the nudge rule and closure already treat them right; the billing badge reads « Aucune carte — Interac »). **Found, not changed:** the sales journal counts a "card" session as collected at closure even when nothing was charged — the ledger channel is derived from the label (`stripe`), and `isLedgerCreditCleared` treats `stripe` as cleared — e.g. a card client with no card on file, until they pay. The admin dashboard route has no spec (this change was verified end to end).
- **2026-09-11** — **[fix] The sales journal counts a card session once its payment came in; both accounting exports print ids and load the model they populate.** `isLedgerCreditCleared`: a `stripe` credit is revenue once the client's payment was received (`paid`, or `refunded` / `partially_refunded` — the refund is its own event), no longer at closure — closure charges nothing without a card or on a declined card. A voided invoice (`cancelled`) and a bank debit still settling (`processing`) are not revenue yet; a credit whose session no longer exists counts as recorded, as before. At deploy this moved none of the 13 card lines of 2026 (the one unpaid, JC-2026-000014, had been moved to Interac earlier that day). **Fixed alongside:** the sales-journal and full-ledger CSVs printed "[object Object]" in `professional_id` / `appointment_id` (a populated reference stringified), and both populated `appointmentId` without loading the Appointment model — on a freshly started server the export answered 500 "Schema hasn't been registered for model Appointment" until another route had loaded it. Both now import it for registration, with a test that fails without the import. ⚠️ **Keep:** a route that `populate()`s a ref must import that model (User is registered by `@/lib/auth`; nothing registers the others for you). **Found, not changed:** 8 of the 13 card lines of 2026 point to sessions that no longer exist (deleted — 7 for one professional, June–Sept, one of 1 $; likely tests) and still appear in the journal; the journal has no refund lines, so a refunded card sale stays listed at its gross.
- **2026-09-11** — **[fix] Deleting a user no longer erases billing records; the sales journal shows refunds.** `DELETE /api/admin/users/[id]` hard-deleted the user's sessions and receipts and, for a client, left each professional's ledger line for sessions that no longer exist: ten such lines (22 June – 4 Sept, 9 for one professional account, 1 for another), eight counted as revenue. It now answers 409 `HAS_BILLING_HISTORY` when the user has a closed, invoiced or paid session (as client or professional), a receipt or a ledger line; the patient and professional pages say so inside the delete dialog ("Désactivez-le plutôt"). An account with no billing history is still deleted. **Sales journal:** new last column `type_ligne` (`vente` / `remboursement`); a card sale refunded during the year becomes a negative line on the refund's date, for Stripe's cumulative amount (the whole price for a full refund recorded before the webhook), capped at the sale (`refundedAmountCad`). ⚠️ **As found, not decided:** no route or webhook reduces the professional's ledger credit on a refund, so the refund line takes the whole amount off `frais_plateforme_cad` and leaves `net_pro_cad` at 0 — the clinic absorbs refunds. If professionals should share them, that is a ledger change (an adjustment row at refund time), not a journal change. **Not changed:** several partial refunds show as one line at the latest refund's date with the cumulative amount; Interac and organization refunds have no journal line (neither reaches the ledger). **Found, not changed:** Interac deposit notifications are stored only since 2026-08-14, so money received by Interac before then cannot be traced from the platform; two paid client receipts (JC-2026-000004, 146 $, and an older 120 $ one) point to deleted sessions — left alone, they are a client's fiscal records.
- **2026-09-11** — **[change] The payer choice in the patient file defaults to the client.** Owner's request: « pour le choix de paiement, mettre par défaut que ce soit le client lui-même qui paie ». It read « Choisir le payeur » (nothing) on a closed session with no payer record and « Selon la couverture » on every upcoming one — even with no coverage and organization billing off — so an admin set « Client » by hand on JC-2026-000014. Now (`payerChoice` in `src/lib/session-payer-choice.ts`): a closed session shows who paid, the client when there is no record; an upcoming session shows the client unless an active coverage applies to its beneficiary (`coverageApplies`, from `GET /api/admin/users/[id]/appointments`), then « Selon la couverture »; choosing the default stores no override (`overrideFor`), so a coverage added later still applies. **Fixed alongside:** while organization billing is off, closure ignores a payer chosen ahead (`planSessionPayers` returns null), yet the upcoming-session dropdown offered « Organisme » and « Hors plateforme » — « Hors plateforme » for a session paid in cash still charged the card at closure. With the switch off an upcoming session shows « Payeur : Client » and no dropdown; after closure the payer can still be changed (the reassign route does not depend on the switch). **Not changed:** an override stored earlier is kept and applies again if the switch is turned on (none exists on an upcoming session in production).
- **2026-09-11** — **[fix, security] Billing and accounting need billing rights, not only the admin role.** « Facturation et paiements » (`/api/admin/billing`), « Comptabilité & cycles » (the 8 routes under `/api/admin/accounting/**`: collections, anomalies, balances, sales-journal, ledger-full, payout-debit, the professional's ledger and payout) and « renvoyer la demande de paiement » (`/api/admin/appointments/[id]/resend-payment`, sent only from those two screens) checked only `role === "admin"`: a support admin without `manageBilling` (cajclinique@gmail.com in production) read every client's payments, the sales journal and the professionals' ledgers, and could record payouts. `POST /api/admin/accounting/professionals/[id]/payout` was looser still: it accepted `managePatients` in place of billing rights, and skipped its check for an admin with no `Admin` record. Closes the « Found, not changed » of the phase 5 entry. All ten now start with `requireBillingAdmin()` (401 without `isAdmin`, 403 without an active `manageBilling`, fails closed); `src/app/api/admin/billing-accounting-guard.spec.ts` builds its route list from the filesystem, so a route added under `billing/` or `accounting/` without the gate fails. **The screens** (display only — every route checks for itself): the admin layout reads the permission once on the server (`getAdminUiPermissions`, `src/lib/admin-rbac.ts` — the gate's rule, never throws, anything unexpected hides) and hands it down (`AdminPermissionsProvider`); without it the sidebar drops Facturation, Organismes payeurs, Factures aux organismes and Comptabilité (`visibleNavSections`, `src/lib/admin-nav.ts`), the two pages show « Accès réservé » (`BillingAccessRequired`) — also when their API answers 401/403 because the right was taken away while the page was open — the professional's page has no « Grand livre » tab (not even from `?tab=ledger`) and the schedule no « Enregistrer un versement ». ⚠️ `isAdmin` comes from the login token (frozen until the next login): an admin whose User lacks `isAdmin`, or who has no active `Admin` record, loses these screens. Checked in production before deploy (read-only): 3 admin accounts, all `isAdmin`, all with an active record — admin@admin.com and sassiessid1@gmail.com keep billing, cajclinique@gmail.com loses it, as the owner decided. **Found, not changed:** `client-receipts/*`, `manual-invoice` and `payment-guarantee-requests/*` accept `manageBilling || managePatients` (outside this change: they sit in the patient file); `payout-debit` records a payout with no check against what the professional is owed (the professional's payout route has one); the « Payeur à confirmer » admin email links to `/admin/dashboard/billing`, which a recipient without billing rights sees as « Accès réservé »; production has 2 `Admin` records whose user is not (or no longer) an admin, and sassiessid1's record carries the role `admin`, which is not in the model's enum.
- **2026-09-11** — **[feature, dark] Spec 002 phase 7 — an organization's own claim form goes out with its invoice.** An organization flagged « exige son propre formulaire » gets nothing until an admin attaches its form (one PDF, filled in by hand) to the invoice or statement — the hourly auto-send included (counted as `autoSendAwaitingForm`; the review email says « formulaire de l'organisme à joindre ») — unless an admin sends without it, which the send log records (`withoutOwnForm`). "Factures aux organismes" shows « Formulaire requis / joint / à revoir », the organization's notes, and attach / replace / remove / download. ⚠️ **Invariants — keep them:** the form leaves only through `deliver()`, after the consent gate and a fresh decision on the invoice as it is at send time (`formForDelivery`), and its bytes must still hash to what was attached; it leaves under a fixed name (`organizationFormFileName`: `JCO-…-formulaire.pdf`), never the uploaded one (it may carry a patient's name); the form check runs **before** anything is reserved or numbered; a form is out of date when the lines it was filled in from changed (`linesFingerprint`: session, date, patient, case number, professional, licence, duration, amount — not the title); only a person may send without it (the cron never passes `withoutOwnForm`); the file is a `StoredFile` of kind `organization-form`, refused by `/api/files/[id]` (`PRIVATE_STORED_FILE_KINDS`) and served only to billing admins by `/api/admin/organization-invoices/[id]/attachment` (`no-store`, `nosniff`, sandbox CSP); an upload needs the « nothing clinical » confirmation, is a PDF by content, ≤ 5 MB, and is refused when the antivirus cannot answer (it leaves for a third party); a draft's form is deleted with the draft or when replaced on it, while an issued invoice's old forms are kept (the record of what was disclosed). The unused `attachmentFileId` is gone. At deploy (read-only check): 0 organization invoices, 0 organizations, switch off, antivirus key set. **Found, not changed:** a form that must carry our JCO number can only be completed after the first send (send without it, then attach and resend); the send log records a send as delivered when SMTP is not configured (existing `sendEmail` behaviour, local only).
- **2026-09-11** — **[feature, dark] Refunding an organization from the invoice screen (spec 002 phase 8).** "Factures aux organismes" → an invoice → « Rembourser » on a payment. For each refund the admin says whether the money is still owed (« toujours dû »: the balance comes back, reminders resume) or no longer owed (« plus dû »: a credit is recorded); the part that only returns an overpayment needs no choice and brings the balance back to 0; a void invoice takes no choice. A card payment is refunded **through Stripe**; Interac, cheque, EFT are recorded as refunded outside (Stripe is never called). A reason is required (internal, never emailed); the organization is emailed by default (`organization_refund`, amount, how, balance, pay link — no reason, no patient). ⚠️ **Invariants — keep them:** what is owed is `balance = total − credited − paid`, where `credited` is the sum of `creditCents` of refunds not failed and is only ever **recomputed** (`recomputeInvoiceMoney`, one pipeline write), never incremented; a credit never takes the balance below 0 (`refundCreditCents`, organization-invoice-money.ts); a Stripe refund writes its `requested` row **before** Stripe is asked, with the idempotency key `orgref_<refundId>`, and records the result through the webhook's own `$max` path — an unknown outcome keeps the row `requested` and « Vérifier » (`checkOrganizationRefund`) adopts or fails it, never re-sends; one unconfirmed refund blocks the next on the invoice; refunds are refused on a disputed invoice; payment rows carry a `paymentId` set where the row is written, never by a schema default; the team is emailed only about refunds not made from the screen (`explainedAdminRefundCents` — a Stripe-dashboard refund) and about failures. **Behaviour changes:** a FULL refund made in the Stripe dashboard now puts the invoice back to awaiting payment (`sent`/`overdue`) and reminders resume — it used to stay « refunded » with its balance owed and no reminder; `refunded` now means closed (all money back, all credited) and is no longer listed under payments to review; a fully refunded invoice can be voided (its sessions go to another payer); voiding waits for any refund to settle. **Verified before deploy (read-only):** the LIVE webhook already receives `charge.refunded`, `charge.refund.updated` and `charge.dispute.created` (only `payment_intent.processing` is missing — bank debit needs it); 0 organization invoices in production. **Found, not changed:** the sales journal counts organization sessions by their `orgStatus`, so a partly credited invoice still counts in full there and organization refunds do not appear in it; a refund made in the Stripe dashboard that should not be owed has no way to be marked « plus dû » afterwards (use « Rembourser » in the screen instead) — a « crédit » action would close that.
- **2026-09-12** — **[feature, dark] An organization can pay an invoice by pre-authorized bank debit (spec 002 phase 9).** With the new « Débit bancaire (DPA) » switch on (Organismes payeurs — off by default, independent of organization billing), the pay link offers card or bank debit: one ACSS debit from a business account for that invoice (`payment_schedule: "sporadic"`, `transaction_type: "business"`, `verification_method: "automatic"`), never kept for another (no `setup_future_usage`: no standing mandate). It takes about 5 business days; meanwhile the invoice carries `pendingDebit {paymentIntentId, amountCents, since}` — the pay page says « Débit en cours », the admin screen shows a chip, a line and « Vérifier auprès de Stripe » (`debit_check`). ⚠️ **Invariants — keep them:** while `pendingDebit` is set there is no reminder (`dunningStepFor`, the candidate query and the claim filter), no overdue flip, no « Enregistrer un paiement » (409 `DEBIT_PENDING`, also in the write filter), no void (same, also in the write filter) and no second online payment (409 `PAYMENT_IN_PROGRESS`, before Stripe is asked); the marker is set only for an ACSS intent not yet recorded, not already bounced (`paymentEvents` `debit_failed` whose `ref` is that intent) and while no other debit is pending; it is cleared by that intent's own success (also on a replay), cancellation, bounce or « Vérifier »; the payment's method is read from the intent (`organizationMethodForIntent`), never assumed « card »; a declined card stays silent; a bounce (the debit was processing, or a charge exists) is recorded once, alerts the team and emails the organization (`organization_debit_failed`: amount, balance, pay link — never the bank's reason) only while it still owes; a failed microdeposit check (nothing debited) is not a bounce; an open intent is reused only for the same amount and method, and a debit that failed is never reused; the idempotency key carries the method and the intent it replaces (`orginv_<id>_<method>_<amount>_<payments>_<previous|none>`), so card → debit → card never replays a cancelled intent. Interac money landing during a debit is still recorded (money already in the account always is): if the debit then clears, the invoice is overpaid and listed for a person, who refunds it (phase 8). A debit refunded through Stripe goes back to the account it came from (the refund email says so). A debit the organization later contests with its bank arrives as a dispute and is flagged like a card chargeback. Receivables: « débit bancaire en cours » on late invoices; still on its way after 10 days → payments to review. `payment_intent.processing` is a new webhook case (organization intents only; an appointment's is logged and left alone — its own routes track it). **Manual steps before the switch is turned on (owner's call):** the LIVE webhook does not receive `payment_intent.processing` yet (verified 2026-09-11) — run `scripts/add-stripe-webhook-events.ts` (it now lists it) with the LIVE key read in place on the server, never copied — the ops copy in `/root/jechemine/scripts/` is older (3 events) and must be replaced first ([HANDOFF §9](../ops/HANDOFF.md)); until then the pay page's confirmation (`POST /api/organization-invoices/pay/confirm`, which asks Stripe) and « Vérifier » set and clear the marker, but a debit whose page closed before confirming would not show as pending and reminders could go out meanwhile. Also check in the Stripe dashboard that pre-authorized debits (ACSS) are active and PAD customer emails are on, then pilot on one small invoice. **Not tested end to end:** Stripe's own ACSS form and the microdeposit path need real test keys — verified with Stripe faked on the SDK (34 checks against a real MongoDB: guards, filters, settle, bounce, replay, reuse), signed webhooks through the dev server (24 checks) and the screens in a browser (FR/EN, 400 px). Checked in production before this branch (read-only): 0 organizations, 0 organization invoices, both switches unset (off).
- **2026-09-12** — **[feature, dark] Showcase city hosts: the routing foundation (spec 003 phase 0).** The showcase pages will live on one host per Quebec city (`psymascouche.jechemine.ca/<slug>`), served by the same app ([ADR-0003](../architecture/decisions/0003-per-city-showcase-hosts.md)). `src/middleware.ts` now applies `routeRequest()` from `src/lib/showcase-hosts.ts`: a known city host is rewritten to the internal segment `src/app/showcase/[cityKey]/…`; the bare domain still 308s to www. Everything sits behind `PlatformSettings.showcaseEnabled` (off; set by `PUT /api/admin/showcase-settings`, which needs `manageProfessionals`): while off, a city page 307s to www and the city's robots.txt is `Disallow: /`; while on, the city page stays `noindex` and its sitemap empty until professionals are published (phase 1). ⚠️ **Invariants — keep them:** (1) routing keys on the Host header only, never the scheme, and every redirect lands where no rule redirects again (spec'd); (2) the internal `/showcase/<city>/…` path is never served under its own name on a public host — it 308s to its city host — and www's robots.txt disallows it; (3) every page of the segment sets an **absolute** canonical on its city host (`showcasePageMetadata`): the root layout's `canonical: "./"` resolves against the rewritten path and would publish `/showcase/…` URLs; (4) `openGraph`/`twitter` are replaced wholesale per level, so the helper always sets the images; (5) hosts we do not serve (an unknown `psy…` host, any other subdomain) redirect with **307**, never 308 — the registry grows, and a browser-cached 308 would outlive the day a city is added; (6) a city host reaches only `/_next/*`, `/api/showcase/*`, `/api/files/*` and the NextAuth session probe — any other API 308s to www; (7) `127.0.0.1`, `localhost` and an empty Host pass untouched (the watchdog, the deploy health check, and Next's image optimizer, which fetches `/api/files` without headers); (8) the switch is read in the city layout and in each route handler — the middleware runs on the edge, without the database; (9) never name the segment with a leading underscore: Next 16 treats `_folders` as private and never routes them; (10) per-host robots.txt and sitemap.xml are route handlers in `robots-txt/` and `sitemap-xml/` (a folder named `robots.txt` or `sitemap.xml` becomes a Next metadata route, which receives no params), so the middleware matcher no longer excludes `.txt`/`.xml`. **Registry:** the Quebec entries of `src/data/canadaCities.ts`; host label = the name without accents or punctuation (`troisrivieres`, `valdor`). Host labels are public URLs, pinned in `showcase-cities.spec.ts`. The list lacked the 17th region: Nord-du-Québec (Chibougamau) added. **Also:** `manageProfessionals` is enforced for the first time (`requireProfessionalsAdmin`, and `AdminUiPermissions.manageProfessionals` for the menu); `SITE_URL` now lives once, in `src/lib/site-url.ts`; a new `messages-lockstep.spec.ts` fails on any FR/EN key drift — it found one older gap, `Auth.memberSignup.reviewPhone` (French only), listed as known until fixed. **Found, not changed:** the site sends `preload` in its HSTS header but `jechemine.ca` is not on the preload list and is not preloadable (plain-http apex redirects to www first; hstspreload.org status `unknown`). The apex's `includeSubDomains` still binds every browser that once opened `https://jechemine.ca`, which is why the wildcard certificate must exist before the `*` DNS record ([HANDOFF §11](../ops/HANDOFF.md)). `docs/architecture/overview.md` still said there was no sitemap/robots and listed a root `loading.tsx` — both fixed. **Verified locally** (production build, `next start`, throwaway MongoDB): 40 HTTP checks by Host header — switch off then on, apex, www, staging, 127.0.0.1, unknown hosts, robots and sitemap per host, canonical, `noindex`, CSP and HSTS present, static assets, the image optimizer, RSC requests, real 404s inside the city chrome — and 12 browser checks through a host-resolver rule (hydration, no console error, 400 px, client-side navigation from the 404 back to the city page); 13 mutants killed. **Not live until** the owner picks a DNS API and the wildcard certificate, the wildcard vhost and the `*` record are installed, in that order ([HANDOFF §11](../ops/HANDOFF.md)).
- **2026-09-12** — **[feature, dark] Showcase pages: invitation, preparation, review, publication (spec 003 phase 1).** « Pages vitrines » (admin, needs `manageProfessionals`) lists every approved professional with the state of their page. An admin invites one (city suggested from the office city, address from the last name): a `ShowcasePage` is created, prefilled from the profile, and the professional is emailed. « Ma page vitrine » (professional, in the menu once invited) edits the page — photo, presentation in French with optional English, values, 3 to 12 expertises from the catalog, order, the two consultations offered — shows what is still missing, and sends it for review with the publication consent. The admin previews the draft or the live version, reads the history, corrects with the same form, approves, asks for changes, takes the page down or puts it back, sends a reminder, or moves it to another address or city. The public page is `psy<city>.jechemine.ca/<slug>`; the city page lists the published professionals and the city's sitemap names them. The catalog screen marks the expertises pages may use, each with its URL segment (`ProCatalogItem.showcase`, `slug`). Six email types (category « Pages vitrines »; the submission alert under « Alertes administratives »). ⚠️ **Invariants — keep them:** (1) public pages read the approved copy only, through `buildShowcasePublicProfile` (`src/lib/showcase-public.ts`), which builds the object key by key — its keys are pinned (`SHOWCASE_PUBLIC_KEYS`) and a poison fixture proves no email, phone, home city, street address, payout details, calendar token, professional rate or margin gets through; never spread a document into it; (2) identity facts — title, permit number, languages, modalities, office city, prices — are read live from `Profile` and the pricing rules, never copied into the page, so the page cannot disagree with the platform; (3) every change is one conditional write on the state it was decided from: approval on the draft revision the admin looked at (409 `REVISION_CHANGED`), submission only while no review is pending, a move on the address and city it read; (4) a page is public only while the switch is on, the page is published and the professional's account is active — `/api/files/[id]` applies the same three conditions to its photo (Next's image optimizer fetches it without cookies); a photo that is not public goes only to the page's professional, its uploader and admins, and a public one is cached an hour, not a day, because it can be withdrawn; (5) publication needs the current consent (`SHOWCASE_CONSENT_VERSION` — bump it whenever `ShowcasePro.consent.text` changes); a page its professional took down comes back only by their hand (an admin can neither republish nor approve over it), one an admin took down only by an admin; (6) an edit after publication goes to the draft and needs a new approval, while the two consultation switches apply at once; (7) an address that was ever public stays with its page and answers with a 308 to the current one, and a page moved to another city 308s from the old host; (8) professionals write plain text only (paragraphs), never rendered as HTML, and structured data escapes `<` (`jsonLdString`); (9) message keys built from lists (titles, orders, modalities, languages, session types, requirements, error codes, badges, history) are checked in both languages by `showcase-messages.spec.ts`, which also refuses `<` in `Showcase*` strings (next-intl reads it as a tag); (10) `showcases-guard.spec.ts` and `showcase-pro-guard.spec.ts` build their route lists from the filesystem: a route added under `api/admin/showcases/` or `api/professional/showcase/` without its gate fails. **Also:** the free-cancellation window lives in one place, `FREE_CANCELLATION_HOURS` in `src/lib/cancellation-policy.ts`, read by the cancel route and the page. **Found, not changed:** (a) every page of the app carries the whole message bundle, serialized by the root layout's `NextIntlClientProvider` — a professional's public page is 417 KB of HTML, most of it French messages (`messages/fr.json` is 427 KB); heavy for a search landing page, to address in phase 2 by giving the city segment only the namespaces its client components use; (b) `PlatformSettings.cancellationPolicy.clientCancellationHours` defaults to 24 h, but the cancel route applies 48 h and ignores the setting — the page shows the 48 h the route enforces; (c) the cookie banner is drawn above dialogs, so until a visitor answers it, it covers the bottom of any open dialog (seen at 1280×900 on the consent and catalog dialogs); (d) the page's « Demander un rendez-vous » opens the booking funnel with `pro` and `city` in the address, which the funnel ignores until phase 3 (direct requests): a visitor is matched as today. **Verified locally** (production build, `next start`, throwaway MongoDB): 132 HTTP checks — guards, invitation, the draft allowlist, photo size, format and visibility, consent version, incompleteness, a stale revision, publication while the pages are closed then open, title, canonical, preview image, structured data and prices, 12 private values absent from the HTML, the photo through the optimizer, the city page, the sitemap and a cross-city 308, changes after publication, the consultation switches, who may take a page down or put it back, reminders, moves and redirects, an inactive account, the catalog flag, previews — and 47 browser checks (the professional edits, saves, accepts the consent and submits; the admin filters and approves, and opens a catalog item; the public and city pages at 1280 px and 400 px, client-side navigation, no console error); 25 mutants killed; 1884 tests. The six emails ran without an error in the server log, but no SMTP was configured locally, so their content was not seen. **Not live until** phase 0's certificate, wildcard vhost and `*` record exist and the owner turns the switch on.
- **2026-09-12** — **[feature, dark] Showcase search pages, the city directory and visit counts (spec 003 phase 2).** The city page `psy<city>.jechemine.ca/` now lists who is presented under a heading built from their titles (« Psychologues et psychothérapeutes à Mascouche »), the city's expertises (links to `/specialite/<slug>`), the nearby cities of the region, frequent questions (FAQPage data) and a matching band; each expertise page lists who carries it. www gets the directory `/psy` (the 17 regions, with the cities where someone is presented) and `/psy/<region>`, linked from the footer and listed in www's sitemap while the pages are open; a city's sitemap now also lists its expertise pages; an unknown `psy…` host now 307s to `/psy`. Anonymous daily visit counts (`ShowcaseDailyStat`, `POST /api/showcase/beacon`) are shown to the professional (« Ces 30 derniers jours ») and in the admin list. ⚠️ **Invariants — keep them:** (1) a city page is indexed only with a professional presented in that city; with none there but some in the region it is served `noindex, follow`; with none in the region it 307s to `/psy/<region>` (`decideCityPage`); an expertise page exists — otherwise a real 404 — only when the catalog offers the expertise with a URL segment and someone in the city carries it; a region page is `noindex` while empty; sitemaps list indexable pages only; (2) every count comes from published pages of **active** professionals in registry cities (`loadShowcaseDirectory`), the same filter as the pages; (3) a heading names titles only when everyone listed has a recognised title, three at most (`headingTitleKeys`), otherwise the generic « Professionnels en santé mentale »; (4) French region names take their own preposition (« dans Lanaudière », « en Mauricie », « à Laval », « sur la Côte-Nord »): `Showcase.regionIn.<regionKey>`, checked in both languages by `showcase-messages.spec.ts` — never write « dans la région de {region} »; (5) **a city host receives only the translation messages its client components read** (`clientMessagesFor`, `CITY_HOST_CLIENT_NAMESPACES` = the global providers' namespaces, checked by a spec that reads `providers.tsx`): a professional's page went from 417 KB to 44 KB of HTML. A client component with translations added to the providers or to a city page needs its namespace listed, or it renders raw keys; (6) **Next runs the middleware twice for a rewrite** — on the public host, then again for the destination on the internal host (`localhost:3000/showcase/<city>/…`), carrying the first pass's request headers. Anything the middleware sets for the render must be set again on that second pass: `routeRequest` returns `{ action: "next", cityKey }` for a known city's internal path on a foreign host, derived from the path, never from a header; `x-showcase-city` is stripped from every other request, so a client cannot set it. A spec of the middleware alone cannot see this; it showed in Next's router debug log (`DEBUG=next:router-server:*`), and `middleware.spec.ts` now replays the second pass; (7) the visit counter stores anonymous daily counts only (no cookie, no IP, no visitor id), ignores crawlers and headless browsers by user agent, is rate-limited per IP, counts a page only when that published page is in the city sent, answers 204 whatever happens to the count and 404 while the pages are off, and is never rendered in previews. **Found, not changed:** (a) www pages still carry the whole message bundle (home 464 KB, `/psy` 423 KB): trimming www needs each route's list of client namespaces — a separate change; (b) the counts are light: a reload counts again, privacy extensions may block `sendBeacon`, and the rate limit lives in one process's memory; (c) Google shows FAQ rich results only for well-known government and health sites since 2023 — the FAQPage data still describes the page; (d) the links from `/psy` to the city hosts are absolute `https://psy<city>…` and resolve only once the wildcard certificate and DNS exist (phase 0's steps); (e) a card without a photo shows an empty grey area — the design pass should give it a placeholder. **Verified locally** (production build, `next start`, throwaway MongoDB with published pages in Mascouche, Terrebonne and Laval, an inactive professional, an unapproved page, an empty city in a populated region and an empty region): 62 HTTP checks — pages off (404s, nothing in the sitemaps, counter 404, no footer link), the unknown host, `/psy` and a region page (headings, canonicals, links to city hosts, structured data, `noindex` when empty, 404 for an unknown region), city pages (headings from titles, region prepositions, counts excluding inactive and unapproved professionals, expertise links, nearby cities, FAQ and breadcrumb data, `noindex` for Repentigny, 307 for Rimouski), expertise pages (title, canonical, who carries it, four 404 cases), the city and www sitemaps, the trimmed bundle (47 KB against 424 KB), counts stored and shown to the admin and the professional — and 26 browser checks (client-side navigation from the directory to a region, and from a city page to an expertise page to a professional; the cookie banner still rendered on a city host; the FAQ; 400 px on four pages; counts from a real browser; the admin column); 24 mutants killed; 1922 tests.
- **2026-09-13** — **[feature, dark] Free times on the pages and requests held until the professional answers (spec 003 phase 3).** A professional's page shows their free times for the next six weeks, two weeks at a time, for each consultation it offers (« Consultation standard », and « Consultation ponctuelle rapide » when switched on). Choosing a time opens the booking funnel on www with the time; the request is proposed to that professional only and the time is held for the client. The professional answers from « Demandes directes » on the proposals page: accepting confirms the first appointment at once (same confirmation and payment invitation as schedule-first), declining asks for a reason the client never sees. A declined request, or one left unanswered past its deadline (24 h, 12 h for a quick consultation, never later than an hour before the time), goes back to the team's queue and the client is emailed another time on the page or Je chemine's matching (`/demande-directe/rejumeler`). The client can withdraw a request still waiting; the calendar shows it as « Demande en attente »; the admin queue marks each state. The quick consultation gets its own price and length per professional (admin pricing editor) and an optional platform price (Settings). Four email types (« Pages vitrines », the team alert under « Alertes administratives »). ⚠️ **Invariants — keep them:** (1) **one computation of free times** (`computeFreeSlots`, `src/lib/available-slots.ts`) for the public page, the booking intake and the professional's schedule modal; days and times are Montréal wall-clock values and instants use Montréal's real offset — a time the clocks skip in spring is never offered; never compute a slot with the server's time zone; (2) **anything busy removes every grid time it overlaps**: scheduled and ongoing sessions (read a day either side, since dates are stored at UTC noon and older rows at midnight) and live holds (`loadOccupiedIntervals`, `src/lib/slot-occupancy.ts`); (3) **the hold is the only lock**: `SlotHold`'s unique index on professional, day and time makes the first request win; its TTL index only collects garbage — a hold past `expiresAt` is free and taken over atomically; a hold carries its length and blocks what it overlaps; it lasts exactly until the professional's deadline; (4) **the booking routes check the time, then hold it, then save** (`prepareDirectRequest`); a failed save gives the hold back; (5) **a direct request is proposed, never assigned**: `routingStatus: "proposed"`, `proposedTo: [pro]`, `professionalId` unset, `directRequest.state: "pending"`; the matcher never sees it (`POST /api/appointments` and `/guest` skip it), and `/accept`, `/refuse`, the generic PATCH refusal, admin assign and admin schedule all refuse it (409 `USE_DIRECT_ROUTES` / `DIRECT_REQUEST_PENDING`); (6) **accepting is one atomic claim** on the pending state, the professional asked and the deadline, and it schedules, prices and frees the hold together; declining, expiry and withdrawal are one atomic claim on the pending state too, so only one of them wins; (7) **a declined or expired request returns to `awaiting_admin` with its date and time cleared**, and the professional joins `refusedBy` unless only the time did not suit (`directRequestExcludesProfessional`); it never increments `cascadeAttempts` — `runProposalTimeouts` expires direct requests first and its cascade query excludes pending ones; (8) **every route that gives a professional's time away also refuses a held time** (409 `SLOT_HELD`): schedule-first, the professional's and the admin's create and move, the admin schedule; their session check stays the exact-start check it was — only holds are checked by overlap there; (9) **the booking intake no longer accepts `professionalId`, `date`, `time` or `duration`**: through the guest route anyone could attach any professional to a request as already accepted, with nothing holding the time; a time from a page arrives as `direct` and is re-checked on the server; (10) the reroute link is 32 random bytes stored as its sha256, valid 14 days, used once, and does nothing until the client presses the button (a mail scanner opening the link cannot use it); (11) the public slots and summary routes carry days, times, length, price and the page's public facts only — never the professional's id, rate or working hours — are rate-limited per IP and 404 while the pages are off; (12) quick consultation pricing: the professional's `rates.quick` when either side is set, else the platform's quick price split by the percentage (never mixed with the solo rate), else exactly an individual session; a quick consultation is booked as `solo` and `isEmergency`. **Fixed on the way:** (a) `GET /api/appointments/available-slots` answered anyone who passed a professional's id, looked for booked sessions at UTC midnight while every row is stored at UTC noon (so the proposals page's schedule modal offered times already booked), and turned a break of 0 into 15 — it now requires the professional's own session or an admin; (b) `/api/appointments/proposed` sent professionals the client's price and the platform's fee; (c) the funnel showed no error raised on its review step (the banner stopped at step 3). **Found, not changed:** (a) two requests for overlapping but different grid times, made in the same instant right after a professional changed their session length, could both be held; accept-direct re-checks sessions by overlap, so it cannot double-book, but the professional would have to decline one; (b) a session created off the grid (manual entry) and a held request are both checked by overlap for the public page but the legacy routes still compare sessions by exact start; (c) the hourly cron detects an expired request up to an hour late (the lazy trigger on the proposals page and the admin queue narrows it to 10 minutes); (d) the client dashboard's home page lists a pending direct request as a pending request with no professional named — only the appointments page explains it; (e) the proposals page's table rows still read `proposedAt + 24 h` for the ordinary accept deadline; direct requests show their own `respondBy`. **Verified locally** (production build, `next start`, throwaway MongoDB): 79 HTTP checks — pages off, free times (grid, lead time, windows, both consultations, a page without the quick one, an unpublished page), the summary, the professional's page, the schedule modal's access rule, a guest request (stored proposed, priced, held until the deadline, the time gone from the page, a second request refused, forged fields ignored), the professional's answer (redacted proposals, ordinary accept and refuse refused, admin re-route refused, accept-direct scheduling at once, the hold gone, the guest's account, a second answer refused), a client's quick request declined (urgent, solo, 30 minutes, quick price; back to the queue, time cleared, reason kept, excluded from matching, time offered again), the reroute link (unknown, used, used twice, the page), withdrawal, expiry through the cron (never cascaded), a held time refused by schedule-first, the professional's move and the admin's booking, quick pricing edited and read back, pages off again — and 41 browser checks (the free times and both tabs, the funnel with a chosen time through to a real submission, the professional's accept and decline dialogs, the calendar chip, the client's withdrawal, the admin badges, the reroute page, 400 px): 40 passed; the decline dialog's own assertion missed its selector while the screenshot shows the four reasons and the decline went through; 28 mutants killed; 183 test files. No SMTP locally, so the emails' content was not seen.
- **2026-09-13** — **[feature, dark] A professional's waitlist (spec 003 phase 4).** On a professional's page, a visitor who finds no suitable time can join that professional's own waitlist — name, email, optional phone, consultation, modality, up to three reasons from the professional's expertises, preferred times of day and days, consent to the current text (`WAITLIST_CONSENT_VERSION`) and a separate consent for text messages — or use Je chemine's general waitlist, which is the ordinary booking funnel (no new rows, no second matcher). When one of the professional's times frees up, the first person in the queue it suits is offered it: the time is held 15 minutes, an email goes out and, only with its consent, a one-segment text message with a short link (`www/la/<token>` → `/liste-attente/reclamer`). Booking it turns the offer into a phase 3 direct request. An unanswered offer is a miss; the third miss ends the place, as do 90 days. The person leaves through the link in the joined email (`/liste-attente/quitter`), even with the pages off. The professional sees « Liste d'attente » (first name and initial, what was asked, the current offer, removal); the team sees « Listes d'attente » (`manageProfessionals`; email and phone only with `managePatients`). Three email types (« Pages vitrines »). New model `WaitlistEntry` (36 models); new job `api/cron/waitlist-offers`, every two minutes. ⚠️ **Invariants — keep them:** (1) **the hold comes first**: an offer takes a `waitlist_offer` hold, then claims the entry (`active` → `offered`), then writes; a lost claim gives the hold back and sends nothing; (2) **a claim is one atomic step** from `pending` to `claiming` while `expiresAt > now`; the offer's hold is *converted* into the request's hold (`convertOfferHoldToRequest`: only a live offer made to that entry), never released and retaken, and the free-time re-check leaves that hold out (`exceptHoldId`); a time that can no longer be requested gives the person their place back without a miss; a claim that crashed half-way is recovered after 10 minutes (converted when its request exists, otherwise back to active); (3) **releasing an offer's hold names its kind** (`kind: "waitlist_offer"`), so an expiry or a removal can never delete a hold a claim already converted; (4) **one open entry per email per professional**: a partial unique index on `isOpen: true` (`one_open_entry_per_email`), and `isOpen` is only written through `waitlistStatusFields`; joining twice answers exactly `{ joined: true }` like a first join, so the form reveals nothing; (5) **queue order is `createdAt` among open entries**, never stored; a miss keeps the position; a time is never offered twice to the same person, a person gets one time at once, and two offers never overlap (`pickOffers`); only times at least 2 h 15 ahead within the next two weeks are offered, so a claim can still be requested; (6) **every route that frees a scheduled time calls `afterSlotFreed` from `after()`** — the appointment PATCH (cancel and move), the professional's and the admin's move and cancel, the admin status override, the change-of-professional auto-cancel, decline-direct and the client's withdrawal — and the job catches anything missed within two minutes; `afterSlotFreed` imports the engine lazily, so routes and their specs never load server-only code; (7) the offer link is 16 random bytes (base64url, to fit the text message), the leave link 32 bytes, both stored as sha256 only; reading an offer changes nothing, and the claim and leave pages act only on a button; (8) the professional's list never carries an email, a phone or a surname; the phone is encrypted at rest; (9) with the pages off nobody joins, reads or claims an offer and no offer is made, but offers, entries and direct requests still expire and leaving still works; (10) a closed entry is deleted 90 days after `closedAt` (`purgeClosedWaitlistEntries`, Loi 25) — while it lives it is the consent record. **Also:** the waitlist job runs `runDirectRequestTimeouts` first, so where the cron runs an unanswered direct request now expires within two minutes (phase 3's gap (c)); the lazy trigger `triggerDueWaitlistOffers` (two-minute throttle) runs off the proposals poll, the admin queue and the public slots route. **Deploy-coupled manual step:** add `*/2 * * * * root /root/jechemine/run-cron.sh waitlist-offers` to `/etc/cron.d/jechemine` ([HANDOFF §7](../ops/HANDOFF.md)) before the switch is turned on — without it offers still move off traffic, but on a quiet night an offer can outlive its 15 minutes. **Found, not changed / choices to confirm:** (a) offers go out at any hour, text messages included: a time freed at 2 a.m. is offered at 2 a.m. and, unanswered, counts as a miss — quiet hours (e.g. 8 h–21 h Montréal) are a product decision; (b) a request made from an offer that the professional then declines or lets expire follows phase 3 (email with another time or matching): the person is not put back on the list; (c) Twilio STOP replies are not read (no inbound webhook): the leave link is the only way off the list, a STOP only stops delivery; (d) no manual « proposer ce créneau » for the professional — offers are automatic; (e) no email when a person leaves by themselves (the page confirms it); (f) the rate limits (5 joins per 15 minutes per address, 3 a day per email, well-formed forms only) live in one process's memory; (g) an entry whose professional unpublishes stays open, with no offers, until its 90 days end. **Verified locally** (production build, `next start`, throwaway MongoDB with a professional booked solid for six weeks, text messages in dry run): 93 HTTP checks — pages off; joins (stored, phone encrypted, hashed leave link, 90 days, duplicate, refused forms, unpublished and unknown pages, both rate limits); lists and access (professional, another professional, client, admin with contact, bad scope); a cancelled session offered to the first person it suits (evenings-only and later people skipped, 15-minute hold, email and text, one segment, token hashed, time gone from the page); the short link, the claim page and the offer read; claiming (converted, request proposed from the waitlist with the person's reasons and modality, hold converted until the deadline, prospect account, a second claim and an unknown offer refused, seen by the professional); a decline passing the time to the next person (no text without consent); expiry through the job (one miss, hold gone, the same time not offered again); a join offered at once; a moved session offered in queue order; the third miss ending the place and the same run offering the time on; leaving with the pages off (once only; both pages `noindex`); removals by the professional (not another's) and by the team; the 50-person cap; the pages off again (links 404, the job runs and offers nothing); the partial unique index — and 30 browser checks (the form on the page: consent error, a real join, 400 px; the slot picker's waitlist link; the claim page's countdown and booking, 400 px; an expired offer; the leave page; the professional's list and removal dialog, 400 px; the team's list and closed tab, 400 px; no browser error). 30 mutants killed — one survived the first run (the engine spec reached the switch check with an empty queue) and was killed once the spec was fixed. 196 test files, 2135 tests. The emails' content was not seen (no SMTP locally); the text message was read in the server log.
- **2026-09-13** — **[feature] Trainings and digital products professionals sell (spec 003 phase 5).** « Formations et produits » (professional) creates a video (YouTube, Vimeo, Dailymotion, Loom), audio (Spotify, Apple Podcasts, SoundCloud), PDF (uploaded), webinar (date, length, room and replay links) or external-link product, French text with optional English, price 5 $–1 000 $ (an external link is free), cover, rich-text body and teaser, and sends it for review with a rights attestation. « Produits » (team, `manageContent`) approves, rejects with notes, or takes it down; approving a live product also accepts its later edits. It sells at `www/book/<slug>` through the existing checkout, reader and entitlements; cards on the professional's showcase page; in the site's library only when the professional asks (`listInLibrary`). The platform keeps `PlatformSettings.productCommissionPercentage` (default 20 %, Settings) and credits the rest to the professional's ledger. Three email types. No new model: a product is a `ContentEntry` of kind "resource" with `ownerProfessionalId`, `productType`, `moderation`… ⚠️ **Invariants — keep them:** (1) **a product's stored `status` is written only by `syncProductLiveStatus`**: published while moderation is approved AND its professional's account is active; the reader also refuses a published product whose professional is inactive, and checkout refuses it before Stripe is asked; (2) **professionals' HTML is sanitized on every write they make and again on approval, never at render** (`sanitizeProductHtml`, sanitize-html allowlist: no style, class, script, frame or form; https/mailto links forced `noopener noreferrer nofollow` in a new tab; images only `/api/files/<id>`); (3) **what they may write is an allowlist** (`PRODUCT_WRITABLE_FIELDS`, `parseProductWrite`): status, owner, slug, moderation are dropped; media links must come from a frameable player (`isAllowedProductMediaUrl`) — direct files are refused; (4) **the paid good is stripped with the body** for anyone who has not bought: `stripPremiumPayload` now removes `webinarAccess` and `productFileId` too; the PDF is a private `StoredFile` kind `product-file` (in `PRIVATE_STORED_FILE_KINDS`), served only by `GET /api/products/[slug]/file` to buyers (`resolveResourceAccess`), its professional and admins, always as an attachment under a fixed name, `no-store`, sandboxed; (5) **the commission is snapshotted on the purchase at checkout** (`commissionBps`, `ownerProfessionalId`, `taxTreatment: "inclusive_untracked"`, also in the PaymentIntent metadata); a settings change never alters a sale; the team's own resources gain none of these keys; (6) **the ledger is settled, not event-driven** (`syncProductLedger`): target = the net of the amount paid while the purchase is paid and not disputed, else 0; the difference with the rows already written becomes one row (`product_sale`, `product_sale_reversal`, `product_sale_recredit`) with a unique `ledgerKey` `product:<entitlementId>:<n>`, so the checkout confirmation and the webhook can arrive in any order, a replay adds nothing and a race writes once; a partial refund keeps access and the share (the platform absorbs it); the webhook settles on grant and on "already-paid", full refund, failed refund and dispute, and a ledger write error throws so Stripe retries; (7) money is integer cents, fee rounded, net = gross − fee; (8) **the team's CMS never edits or deletes a professional's product** (409 `PRO_OWNED`) and does not list it; a product with a paid or refunded purchase cannot be deleted, only taken down; buyers keep access to a taken-down product (the reader shows it to them with a notice, and the PDF still downloads); (9) a professional's product image is a public `StoredFile` kind `product-image` (PNG/JPEG/WebP ≤ 2 MB, never SVG/GIF); both upload routes refuse when the antivirus cannot answer; (10) professionals see ledger `source` and `productSlug`, never the gross or the fee. **Fixed on the way:** production's CSP had no `frame-src` for the players, so every YouTube, Vimeo and Spotify embed — on `/medias` and on the team's premium videos — was blocked; the players' hosts are now allowed (`next.config.ts`, kept in step with `PRODUCT_FRAME_HOSTS`). **Found, not changed / owner decisions:** (a) **taxes** — prices are shown « tout inclus » and the platform does not collect or remit TPS/TVQ on products; the commission is on the gross (the plan's open decision 2); (b) an edit to a live product is visible at once and only flagged for the team, which can accept it or take the product down — never reject the edit alone (the plan's default; the strict variant would hide edits until approved); (c) no webinar reminder emails yet — added by the follow-up entry below; (d) a refund made after the professional was paid leaves a negative balance, carried to the next payout (the payout route already refuses a balance ≤ 0); (e) a professional deactivated with live products: the products stopped selling and disappeared at the next read (the reader and checkout check), but their stored `status` stayed published until something called `syncProductLiveStatus` — fixed by the follow-up entry below; (f) the rich-text editor's toolbar labels were English (existing component) — translated by the follow-up entry below; (g) the professional's terms of sale for products and the rights attestation wording need the owner's validation. **Verified locally** (production build, `next start`, throwaway MongoDB; money through SIGNED Stripe webhook events — checkout itself needs real Stripe keys): 76 HTTP checks — creating (address from the title, next address when taken, unknown type), editing (sanitized at write, forged fields dropped, English following French, price mirrored, bounds, media link per type, another professional 404, client refused), files (PDF by content, private and never on `/api/files`, image public, SVG refused), review (attestation required, no edit under review, the professional's preview, the queue, reject needs notes, approval re-sanitizing what reached the database), the public page (byline, paid text and file link absent, price and teaser, file 403, library only on request, live edits flagged and accepted, CMS `PRO_OWNED` for edit, delete and listing), a sale (paid, 15,20 $ credited once with its key, replays and a second delivery adding nothing, an unsigned event refused, the buyer's token opening the product and the PDF as a no-store attachment, a wrong token 403, the professional's ledger without gross or fee, the sales journal `vente_produit`), a partial refund (nothing moves), a full refund (access revoked, reversal), a failed refund (access and share back), a dispute (share back to 0, four distinct keys), the showcase cards, taking down (gone for the public and the showcase page, a buyer still reads and downloads, no delete with sales), back after review, an inactive professional (not public, checkout 404 before Stripe), an external product (never priced, https only), the CSP — and 17 browser checks (the professional's list, creating from the dialog, the editor, the team's screen, the product page and the showcase page with cards, each at 400 px, no browser error apart from the local build's missing Stripe publishable key in the existing buy button). 21 mutants killed (commission, live rule, external price, media hosts, moderation actor, HTML links and images, dispute, ledger keys and races, sanitizing at write and approval, attestation, delete with sales, notes, webinar room, inactive owner at checkout, snapshot, PDF access and disposition, CMS guard); 211 test files, 2280 tests. The Stripe webhook spec gains the product settlement cases. The emails' content was not seen (no SMTP locally).
- **2026-09-13** — **[feature] Products follow-up: webinar reminders, products that follow their professional's account, the editor in French (spec 003 phase 5).** A webinar's buyers get an email the day before and an hour before it starts (`product_webinar_reminder`, under « Ressources »): the title, when in Montréal time, how long, with whom, and a button to the webinar's page, where the room link is; a guest's button carries their access token. A professional's products now leave the site as soon as their account stops being active, and come back when it is active again: self-deactivation, the team's account activation switch, the user editor's status, « Valider », erasure (`/api/admin/data-lifecycle`) and deleting the account all call `syncProfessionalProducts` after their write. New job `api/cron/products`, every ten minutes (`lib/product-jobs.ts`), also run off the proposals poll and the admin queue (`triggerDueProductJobs`): it first puts right any product whose stored status disagrees with its moderation and its professional's account (`reconcileProductLiveStatus`), then sends the reminders. The rich-text editor (`ContentEntryEditor`, used by professionals' products and the team's content) now speaks the reader's language: toolbar labels and tooltips, the link prompt, and upload errors worded from the upload routes' statuses (`ContentEditor` messages). ⚠️ **Invariants — keep them:** (1) **a reminder is claimed on its purchase before its email goes out** (`webinarRemindersSent`, `$addToSet` guarded by `$ne`) and given back (`$pull`) when the email fails, so two runs never send it twice and a mail outage only delays it; (2) **the key carries the start it announced** (`day:<ISO>`, `hour:<ISO>`), so moving a webinar reminds its buyers again; (3) **only the latest open window counts** (`webinarReminderDue`): the day reminder from 24 h before, the hour reminder from 1 h before, nothing once the webinar has started, and nothing for a purchase made after that window opened (the purchase email has just gone out); (4) only paid, undisputed purchases of a webinar that has a room link and whose professional's account is active are reminded; a guest whose link was revoked is skipped; one run sends 200 emails at most, the rest go on the next; (5) **the email links to the webinar's page, never to the room**, so a refund or a new room link is honoured; (6) the key is absent until a first reminder (`default: undefined`): the team's resources never gain it; (7) **every route that changes a professional's account status syncs their products after the write**, and a failed sync never fails the change (logged; the job repairs it); (8) a product's stored `status` is still written only by `syncProductLiveStatus`. **Deploy-coupled manual step:** add `*/10 * * * * root /root/jechemine/run-cron.sh products` to `/etc/cron.d/jechemine` ([HANDOFF §7](../ops/HANDOFF.md)); without it reminders go out only off dashboard traffic, and an hour-before reminder can be late on a quiet evening. **Found, not changed:** (a) the account verification routes (`verify-email`, `verify-phone`, `set-password`) move an account from pending to active without a sync: a pending account's products are already off sale, and the job puts them back within ten minutes; (b) the shared email layout writes detail labels as « Label : » in English too, in every English email with details; the reminder's plain-text version uses each language's colon; (c) reminders go to buyers only, not to the professional running the webinar; (d) a webinar cannot be cancelled on the platform: taking it down stops sales, it does not cancel it, so its buyers are still reminded, and a professional who cancels has to tell them. **Verified locally** (production build, `next start`, throwaway MongoDB, and a local SMTP sink, so this time the emails were read): 46 HTTP checks — the job's secret; three webinars (within the hour, tomorrow, in thirty hours) and nine purchases: the member's French reminder (Montréal time, length, professional, link without a token), the guest's English one (English title, token link, marked personal), the hour-before reminder, nobody else (revoked link, refunded, disputed, bought inside the window, more than a day away), keys recorded, the links opening the room for buyers and keeping it hidden otherwise, a second run silent, a moved webinar reminding again, the mail server down (claim given back) then back (sent); a professional deactivating their own account, reactivated and deactivated by the team, set active and inactive in the user editor, then erased; another deleted; a third changed straight in the database and put right by the job, then « Valider » putting the product back; the proposals poll running the job; the settings — and 20 browser checks (French and English toolbars, tooltips and link prompt on the product editor, the reminders hint, three refused uploads explained in the reader's language and a valid image inserted, 400 px, the team's content editor, the email toggle under « Ressources », no browser error; the three emails rendered and looked at). 25 mutants killed; 216 test files, 2322 tests.
- **2026-09-14** — **[feature, off by default] TPS and TVQ added at checkout on online sales.** Owner's decision: Je chemine collects TPS and TVQ, added on top of the displayed price, on professionals' products and the team's premium resources alike, at rates an admin sets. Admin → Paramètres → « Taxes sur les ventes en ligne » (`PlatformSettings.salesTaxes`: switch, TPS 5 % and TVQ 9,975 % by default, the two registration numbers). `PUT /api/admin/settings` refuses a rate outside 0–20 % or with more than three decimals, a number with anything but letters, digits, spaces and hyphens, and turning taxes on without both numbers. When on, `POST /api/resources/[slug]/purchase-intent` charges price + TPS + TVQ (`lib/sales-taxes.ts`: integer cents, each tax rounded half up) and the purchase keeps `subtotalCents`, `tpsCents`, `tvqCents`, the rates and the numbers (`taxTreatment: "added"`); a reused pending row drops the tax fields an attempt does not charge; the idempotency key follows the total. `amountCents` stays what was charged, so the webhook's short-payment guard (`AMOUNT_UNDERPAID`) compares against the total. The professional's share and the commission come from `subtotalCents` (`lib/product-ledger.ts`); their sale email shows the price before taxes; the buyer's receipt gains TPS and TVQ lines with the numbers — added outside the editable template, so a customized template still shows them; the sales journal gains `tps_cad` / `tvq_cad` (negative on a reversal); the admin report counts resource revenue before taxes. `/book` says « plus TPS et TVQ, ajoutées au paiement » and the payment window shows price, TPS, TVQ and total (an estimate from the page's rates, then the server's figures once the intent exists). ⚠️ **Invariants — keep them:** a past sale's taxes are never recomputed from today's settings (receipt, journal and ledger read the purchase); the commission is always on the price before taxes; checkout computes everything server-side from the database price. **Open, for the accountant — not handled:** every buyer pays the Quebec rates (no billing address is collected, so a buyer in another province is not charged HST or their own rate); TPS/TVQ on the commission charged to professionals; a partial refund keeps the purchase paid and changes no tax column. (The team's own resource sales, absent from the sales journal at first, were added the same day — entry below.) **Found locally, not changed:** opening the payment window without `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` throws inside Stripe.js (`loadStripe` reads the key with `.match`) — production sets the key. **Verified:** unit tests for the arithmetic, the settings API, the checkout (taxed, untaxed, stale row, reused intent), the ledger and the journal; locally through the production build, signed Stripe webhook events and an SMTP sink, 32 checks (settings guards, the notes on `/book` with taxes on and off, a taxed product sale — ledger 39,20 $ on 49 $, receipt with both numbers, the professional's email before taxes — a payment of the price alone refused, a taxed team resource sale, the journal's tax columns, a full refund reversing them, a purchase from before taxes unchanged) and 15 browser checks (the settings section and its live example, the switch refusing without numbers, the payment window's breakdown, the professional's « Prix (avant taxes) »); 10 mutants killed; 218 test files, 2362 tests.
- **2026-09-14** — **[fix] The sales journal lists the team's own premium resource sales.** The journal (`GET /api/admin/accounting/sales-journal`) was built only from professionals' ledger lines, so a sale of one of the team's own resources on `/book` — no professional, hence no ledger line — reached Stripe, the purchase, the receipt and the admin report but never the accountant's export, nor the TPS/TVQ collected on it. The route now also reads those purchases (`ResourceEntitlement` without `ownerProfessionalId`, status `paid` or `refunded`, `paidAt` or `refundedAt` in the year): a `vente_ressource` line on the day it was paid (the resource's slug in `acte`, the price before taxes as both `brut_cad` and `frais_plateforme_cad`, `net_pro_cad` 0, TPS and TVQ from the purchase or empty for one bought while taxes were off) and, for a full refund, a negative `remboursement_ressource` line on the refund's day; a sale and its refund falling in different years each go in their own year. A professional's product is never listed this way — its lines still come from their ledger. **Not covered:** a partial refund (the purchase stays `paid` with `refundedAmountCents` but no refund date, so there is nothing to date a line by) and a dispute on a team resource. **Verified:** 18 route specs (sales, refunds, years, untaxed, query), 8 mutants killed, 218 test files and 2367 tests, `tsc --noEmit` clean (it also caught a mock typed too loosely in `route.sales-taxes.spec.ts` from the entry above — `next build` does not typecheck spec files), and 11 checks on the production build: the CSV downloaded, a taxed sale, a refunded one on two dates, one from before taxes, pending and failed purchases left out, last year's sale only in last year's export, a professional's product not duplicated, date order, billing rights still required.
- **2026-09-14** — **[design] A professional's showcase page in the « vitrine » design (Claude Design export « Vitrine Amel Sassi »).** `ShowcaseProfileView` is rewritten section by section: top bar (verified by Je chemine, order and permit, other professionals in the city), the professional's own sticky header (`vitrine/VitrineHeader`: section links, current section, reading progress), hero with portrait, trust strip, « À propos » with an « En bref » card, approach + the four steps of a request, values band, fees (standard highlighted, quick), free times (`vitrine/VitrineBooking` replaces `ShowcaseSlotPicker`: service tabs, five days at a time, a time to pick, then « Votre demande » with the request link) beside the waitlist, products, expertise tags linking to `/specialite/<slug>`, FAQ, closing call, 988 band, footer, and a phone-only bottom bar. Fonts Newsreader + Figtree through `next/font` (self-hosted, CSP `font-src 'self'` unchanged), scoped to this page (`vitrine/fonts.ts`); the rest of the site keeps its look. Pure helpers in `lib/showcase-vitrine.ts` (anchors, which sections show, the headline price, the five-day window). Owner's decisions: **the design first, the new fields after** — sections needing content the platform does not hold yet (motif cards, parcours, quote, method cards, value descriptions, tagline) are left out until « Ma page vitrine » collects them, and the FAQ and the four steps are written once by Je chemine, filled with the professional's name, modes and cancellation hours; **no contact block** (no form, phone, address, hours, nor the free 15-minute call); **only showcase pages** change. The city landing and expertise pages moved into a `(city)` route group whose layout draws Je chemine's `ShowcaseHeader/Footer`; the `[cityKey]` layout draws nothing, so a professional's page has only its own header; `[cityKey]/not-found.tsx` adds Je chemine's chrome itself, reading the city from `x-showcase-city`. ⚠️ **Keep:** `data-showcase-cta` on every link to the funnel (beacon counts); the anchors `disponibilites` / `liste-attente` (emails and the funnel link to them); no claim the platform cannot back (no « taxes incluses », no ratings, no testimonials); the scroll animation (`vitrine/VitrineMotion`) only hides what is below the fold and is off with reduced motion; client components on city hosts use only `CITY_HOST_CLIENT_NAMESPACES` (`client-messages.spec.ts` lists the new ones). **Verified:** `tsc --noEmit`, build, lint clean on the touched files, the vitrine/client-messages/showcase-messages specs, and in headless Chrome on `psymascouche.jechemine.ca`: all sections in order, Newsreader applied, a time picked filling the request card, no horizontal scroll at 1280 px and 400 px, no browser error.
- **2026-09-14** — **[design] The showcase page redesigned wide, editorial and rounded, with real ambience photos.** After the first vitrine pass the owner asked for a page that uses wide screens, bigger fluid type and rounded pill shapes. `ShowcaseProfileView` now runs up to 2240 px with `clamp()` gutters and fluid text classes (`#vitrine .vt-xs…vt-lg`, defined in the page stylesheet); fonts are Fraunces (SOFT axis) and Plus Jakarta Sans (`vitrine/fonts.ts`, with `word-spacing: .06em` to widen Jakarta's narrow spaces). The top strip and the emergency notice were removed on the owner's request (the crisis numbers 911 / 988 are therefore no longer on the page — owner informed, a quiet footer line was offered and is undecided). `VitrineHeader` is a floating pill bar carrying the Je chemine logo, the professional's avatar and section pills (smooth scroll, reduced-motion respected). `VitrineBooking` is one three-step card (service → day and time grouped by `groupSlotsByPeriod` → dark summary with the request link); the waitlist is a wide row below it. Expertise cards link with a **relative** `specialite/<slug>` so they work both on the city host and at the internal `/showcase/<city>/<slug>` path a local server serves (an absolute `/specialite/…` 404'd at localhost). Photos: six people-free Unsplash photos self-hosted under `public/showcase/ambiance/` (licence and photographer per file in `LICENSES.md`); `src/lib/showcase-imagery.ts` picks them deterministically per slug, wide ones for the band and closing slots, tall ones for « À propos », office photos first when they exist. ⚠️ The originals total ~8.8 MB in the repo; `next/image` serves resized copies, but replacing a photo should keep it under ~2400 px. Office photos uploaded by professionals (plan phase B) are not built. `showcaseEnabled` stays off.
- **2026-09-14** — **[workflow change] Showcase pages: the admin activates, prepares and publishes; the professional then edits the live page.** Owner's decision replacing « invite → the professional fills and submits with consent → admin reviews ». Now: « Activer la page » (`activateShowcase`) creates a draft prefilled from the profile and sends **no** email; the admin fills it with the existing editor; « Publier » (`publishShowcase`) needs the professional's agreement at `SHOWCASE_CONSENT_VERSION`, either on record or confirmed by the admin in the dialog (`consentAttested: true`), which writes `consent { acceptedAt, version, source: "admin", attestedBy }`. Once a page has a published copy, the professional's saves go **live**: `saveShowcaseDraft`/`setShowcasePhoto` with `actor: "professional"` write `draft.<field>` and `published.<field>` together, conditional on the `draftRevision` they read (409 `CONFLICT` otherwise), refused before publication (409 `IN_PREPARATION`) and refused when they would add a missing requirement to the public copy (422, only newly missing items — a permit missing from the profile does not block text edits); the professional can replace the photo but not remove it. `normalizeShowcaseDraft(…, "professional")` ignores `SHOWCASE_ADMIN_ONLY_FIELDS` (`cityKey`, `orderCode`, `orderLabel`) without reading them; the slug was never writable. Every real change adds an `edit` history entry, and the team gets `admin_showcase_updated` at most once per page per hour (`changeAlertedAt`, claimed with a conditional write). Removed: the submit route, `submitShowcase`, `requestShowcaseChanges`, `remindShowcase`, and the invitation / « to review » / changes-requested email builders. ⚠️ **Kept on purpose, to clean later:** the email types `showcase_invitation`, `showcase_reminder`, `showcase_changes_requested`, `admin_showcase_submitted` stay in `PlatformSettings` and Settings (marked « plus envoyé ») so saved settings keep their shape; the `review` sub-document, its index and `remindedAt` stay on `ShowcasePage` (publishing sets `review.state` to `none`); the history actions `invite`, `submit`, `request_changes`, `remind` stay for older entries. ⚠️ **Known edge:** if an admin saved corrections to a live page and has not published them, the professional's editor shows the draft, so their next save also publishes the admin's pending text for the fields it sends (never city or order); `publishedRevision` is only advanced when the copies were in step. No data migration: a page still `invited` publishes like a draft. `showcaseEnabled` stays off.
- **2026-09-15** — **[feature, dark] Showcase pages: highlights, a quote, « Parcours », method cards, « Ce que j'accompagne » cards and value descriptions.** The owner's « new fields later » from the Claude Design export « Vitrine Amel Sassi », now in the draft/published content (`ShowcasePage` content: `quote`, `highlights[]`, `credentials[]`, `focusAreas[{title, body}]`, `methods[{name, title, body}]`, and `values[].details`), edited in the same editor by the admin and, once published, live by the professional. Rules in `normalizeShowcaseDraft` (limits in `SHOWCASE_LIMITS`: 4 highlights of 60, 6 credential lines of 120, 4 focus cards and 4 method cards, value description 120, quote 240): an item or a card without its French first part is dropped, a list over its limit answers `TOO_MANY_ITEMS`, and the public builder (`buildShowcasePublicProfile`, keys pinned in `SHOWCASE_PUBLIC_KEYS`) applies the same French-required rule so an English-only fragment never reaches a page. The page shows highlights as hero pills, the quote (signed with the display name) and « Parcours » in « À propos », method cards in « Approche », focus cards above the catalog expertises, and a « Valeurs » section only when at least one value has a description. `changedShowcaseFields` now compares objects with sorted keys, so a card resaved with its parts in another order is not reported as a change (it would have sent the team a false « page modifiée » alert). No data migration: existing pages simply have none of these fields. Verified: 2394 tests, build, and 56 browser checks (admin fills and publishes, each block on the public page at 1440 and 400 px, the professional's live edit of a method card).
- **2026-09-15** — **[feature, dark] Showcase pages: office photos.** A page can carry up to 6 photos of the office (`officePhotoFileIds` in the draft and published copies, StoredFile kind `showcase-photo`, same upload rules as the portrait through `storeShowcasePhoto`: JPEG/PNG/WebP, 5 MB, 400 px minimum side, refused when the scanner errored). They fill the page's large image slots in order (band, « À propos », closing) before any ambience photo (`pageImages`), described as « Le bureau de {name} ». Routes: `POST /api/professional/showcase/office-photos` and `DELETE|PATCH …/office-photos/[fileId]` (`{ direction: "up" | "down" }`), mirrored under `/api/admin/showcases/[userId]/office-photos`. Same workflow as the text: an admin changes the draft (public when they publish); the professional changes the live page, pinned to the revision read (409 `CONFLICT`), refused before publication (409 `IN_PREPARATION`), with an `edit · officePhotos` history entry and the throttled team alert. A file is deleted when it cannot be added (limit → 409 `OFFICE_PHOTO_LIMIT`, page in preparation, conflict) and when no copy shows it any more, including when a publication drops it; a photo only the draft removed stays while the public copy shows it. `/api/files/[id]` treats an office photo like the portrait: public only on a published page while the pages are open and the professional's account is active; otherwise only the page's professional, the uploader and admins. The professional's `[fileId]` route reads its context only after the gate, so the guard spec (which calls it without one) proves nothing is touched. ⚠️ Still open, now wider: showcase photos (portrait and office) are not deleted when an account is erased (`data-lifecycle.ts` has no showcase handling). Verified: 2406 tests, 15/15 mutants, build, 47 browser/HTTP checks.
- **2026-09-15** — **[feature, dark] Showcase city pages in the « Ville » design.** The city page (`psy<city>.jechemine.ca/`) has a photo hero (a wide ambience photo fixed per city), a directory with search and « En personne / En ligne / Consultation rapide » chips (`ShowcaseCityDirectory`, filtered in the browser by `matchesDirectoryFilters`), horizontal cards with years of experience, ways of consulting and « Prochains créneaux » (the first time of each of the next two free days, `nextSlotLabels`), a themes grid, a two-column FAQ, a dark matching band and a phone bottom bar. The expertise page and the www region page reuse the same card (`ShowcaseDirectoryCard`, strings prepared server-side by `buildDirectoryItems`, so the client directory needs only the small `ShowcaseDirectory` namespace on city hosts). `ShowcaseCard` gains `yearsOfExperience` and `offersQuick`. Free times come from `loadNextShowcaseSlots`, which reads each listed professional's bookable times (standard, else quick) for at most 24 professionals per render and shows none for a calendar that fails: ⚠️ that is one slot computation per professional on every city page view (the pages are `force-dynamic`); fine at today's handful per city, to cache if a city grows large. Owner decision: no crisis box on these pages; the footer's 988/911 note was removed (the FAQ « Que faire en cas d'urgence ? » answer stays). Verified: 2412 tests, build, 23 browser checks (desktop 1440 and phone 400, filters, search, reset, no horizontal scroll, expertise page, www region links, no console errors).
- **2026-09-15** — **[change, dark] Showcase pages move to www.jechemine.ca/<full name>; city pages removed.** Owner's decision the same day: no subdomains for now (kept for a later use), each professional's page at `jechemine.ca/amel-sassi`, and every city directory removed (city page, its expertise pages, www `/psy` and `/psy/<region>`, the footer's « Trouver un psy par ville » link) — the « Ville » redesign committed earlier that day (`72677da`) goes with them. The page is the top-level segment `src/app/[proSlug]` (404 while `showcaseEnabled` is off; a former slug 308s to the current one). Next serves the site's routes first; `src/lib/showcase-slug.ts` reserves every top-level route name and `showcase-slug.spec.ts` reads `src/app` and `public/`, so ⚠️ **a new top-level route must add its name to `RESERVED_SHOWCASE_SLUGS`** or the tests fail. The middleware sends every `*.jechemine.ca` host other than www, city hosts included, to the same path on www (307), and marks a single-segment unreserved path with `x-showcase-page` so the root layout still sends such a page only its own client messages (a 404 there renders the root not-found, server translations only). Every link that pointed at a city host (emails, waitlist offers, products, direct requests, the editor, the admin screens, JSON-LD, metadata, the sitemap) uses `showcasePageUrl(slug)`. New slugs default to the full name (`amel-sassi`, `amel-sassi-2`…); existing slugs are unchanged — no production page exists (the branch is not deployed), the local test pages were renamed. Admin wording no longer promises city addresses. ADR-0003 carries the amendment; HANDOFF §11 notes the hosts are unused. ⚠️ **Open for the owner:** the Répertoire des municipalités (Données Québec, CC BY 4.0) credit was shown only on `/psy`; the city list still feeds the funnel's city search and the page's city, so the credit needs a new place (e.g. the legal or « Qui sommes-nous » page). Verified: 2368 tests, tsc, lint, build, 17 HTTP checks (new address 200, old slug 308, site pages 200, unknown slug, `/psy` and `/showcase/…` 404, city host 307 and apex 308 to www, canonical on www, sitemap lists the pages, no site-wide messages on the page) and 7 browser checks (desktop and phone, no city or specialty links, no console errors).
- **2026-09-15** — **[feature, dark] Showcase pages: each professional customises their page.** Owner's answers the same day: professionals change section titles and intros, consultation descriptions and the closing band (17 texts), show, hide and reorder the sections below the image band, pick an accent colour from an approved palette, and choose the ambience photos per slot from Je chemine's library; live at once with the usual team alert. Stored with the content (`texts`, `sectionOrder`, `hiddenSections`, `accent`, `ambience` in `draft`/`published`), so the live-edit, history and publication paths apply unchanged. Rules in `src/lib/showcase-customization.ts` (pure): a text without French keeps the page's wording; unknown keys never pass (`normalizeShowcaseDraft` allowlist, `buildShowcasePublicProfile` reads key by key); « Services et tarifs » and « Disponibilités » cannot be hidden; the default order and the default colour are stored as empty, so an editor save that chose nothing reports no change (`changedShowcaseFields` treats an empty choice object as blank). The page draws its sections from `visibleSections` in the professional's order (the header links follow), and the teal is a CSS variable (`--vt-accent`, `-dark`, `-soft`, teal fallback) across the page, the booking panel, the header and the waitlist form. Office photos still come before chosen library photos; an automatic slot never repeats a chosen photo. Editor: « Personnaliser la page » in « Ma page vitrine » and on the team's screen. ⚠️ **Dev trap:** after a schema change, a running `next dev` keeps the old Mongoose model (`mongoose.models` survives hot reload) and silently strips the new paths on save — the history even records the edit. Restart the dev server after editing a model; production starts fresh. Verified: 2388 tests, tsc, lint, build, 18 browser checks (editor: colour, hide, reorder, custom title, photo, save live; public page: colour applied, section hidden, order and header links, title, photo; reset) plus the page and fees regression checks.
- **2026-09-15** — **[change, dark] Showcase pages: the city is no longer chosen, it follows the office address.** Owner's yes the same day, after the city pages were retired. `ShowcasePage.cityKey` stays (bookings, waitlist, stats and the « {city} » wording read it), but nobody picks it: `activateShowcase` takes `matchShowcaseCity(profile.officeAddress.city)` and answers 409 `OFFICE_CITY_UNKNOWN` when the address names no listed city (the team corrects the profile first); `publishShowcase` and `republishShowcase` re-read the office address and move `cityKey` to the city it names now (the « city » requirement means a recognised office city). Removed: the city select in the activation dialog, the team's editor and « Changer l'adresse » (slug only now), the « Villes en ligne » card, `draft.cityKey` handling in `normalizeShowcaseDraft` (a body city is ignored for everyone), `showcaseCityKeyOf` / `requestedShowcaseCityKey` and the pending-city notices. Old drafts may still carry `draft.cityKey`: nothing reads it. Also fixed the same day: a steps-only « Approche » section dropped the professional's own section title (`approachHeadings`). Verified: tests, tsc, lint, build, and a 102-check browser walk-through from no page to activation, preparation and customisation by the team, publication, live customisation by the professional, the public page after each step and the team alert.
- **2026-09-15** — **[fix, dark] Showcase pages and products, full end-to-end runs.** Two browser walk-throughs on the dev server (MongoDB 27999, SMTP sink): the page from no page to activation, the team filling every field (portrait, office photos added/moved/removed, texts FR + EN, highlights, background, methods, focus cards, values, insurance, expertises, services, customisation), publication, the public page (desktop, phone, English), the professional changing every kind of field live, the team alert and the refusals — 206 checks; products from creation to refusal with notes, correction, approval, the public product page and the card on the professional's page, a purchase through a signed Stripe event (paid, ledger net, buyer's content and PDF), changes back to the team and taking down — 89 checks. Bugs they found, fixed with tests: (1) the editor's grey hint for « À propos » promised « {name}, à {city} » while the page shows « {title} à {city} » (or with years): one rule `aboutHeadingMessage`, the editor view now carries `titleKey`/`titleLabel`/`yearsOfExperience`; (2) prices with cents read « 24,5 $ » on the page and in the booking panel: `formatShowcasePrice` (whole dollars without decimals, else two). ⚠️ While fixing (1), a value re-exported from `showcase-public` came through as undefined in the running dev server (« is not a function », « Ma page vitrine » answered 500) although tsc and the mocked service spec passed; the title rule now lives in `src/lib/showcase-title.ts`. Cause not proven (no import cycle found; likely a stale dev module): after touching a module the service imports, call the route in the running app, not only the specs.
- **2026-09-15** — **[change, dark] Waitlist: quiet hours and declined requests back on the list (owner's « go ahead with ur recommendation »).** (1) No waitlist offer goes out between 21 h and 8 h Montréal time (`isWaitlistQuietHours` in `waitlist-rules.ts`, checked in `offerFreeSlotsForProfessional`, so the cron and « a time was just freed » both wait): freed times stay free and the job offers them from 8 h. A time freed late in the evening for early the next morning may no longer be offerable by then (the 2 h lead still applies). (2) When the professional declines a request that came from a waitlist offer, `releaseDirectRequest` reopens the entry (`converted` → `active`, `closedAt` unset): `createdAt` is kept, so the person is back at their original place, and the declined time stays in `offers`, so it is never offered to them again. Not reopened when the place would have ended (`expiresAt` passed) or when the person joined the list again meanwhile (the one-open-entry-per-email index answers E11000; the new place stands). Only a decline reopens: an expired or withdrawn request does not (owner decision named the declined case). The client's « créneau non disponible » email says they keep their place (`backOnWaitlist`). Reopening can take a list one over its cap of 50. (3) `employee` accounts: the decision is « calendar only », but no screen shows employees anything today (`/appointment` sends them to /login), so nothing changed; apply it if an employee screen is ever built. Verified: tests (quiet hours in summer and winter, offers at 22 h and 8 h, reopen / not reopened / duplicate / page source / expired / withdrawn, the email flag), 5 mutants.
- **2026-09-15** — **[feature, dark] Professionals' articles (owner's « go ahead with ur recommendation »).** An article is a pair of `ContentEntry` rows (fr, en) of kind `nouveaute` with `ownerProfessionalId` and the products' moderation block — no schema change. Rules `src/lib/article-rules.ts` (title/summary/text required, `attestation` = « ni témoignage ni promesse de résultat » ticked when sending), service `src/lib/articles.ts` (reuses `productTransition`, `productIsLive`, `sanitizeProductHtml`), routes `/api/professional/articles/**` (requireShowcaseProfessional) and `/api/admin/articles/**` (requireContentAdmin), screens « Mes articles » and « Articles à vérifier ». The team approves, sends back with notes, takes down, and features an approved article in « Nouveautés » (`listInLibrary`, which `listPublishedContent` already honours; taking down clears it); a live edit stays online and returns to the queue (« Modifications à relire »). Stored `status` only via `syncArticleLiveStatus`; `syncProfessionalProducts` also syncs articles (every account-change caller) and the products job reconciles them. Public: `/nouveautes/<slug>` shows the byline (`productByline`) and a « ne remplace pas une consultation » note for owned articles, and lets only the author or an admin read a non-public one (preview banner, noindex); the team's content routes still refuse owned rows (PRO_OWNED). The professional's page gets an « Articles » section (`SHOWCASE_SECTION_KEYS` + `articlesTitle`; stored section orders keep working because `resolveSectionOrder` appends missing keys). New emails `article_moderation_decision`, `admin_article_submitted`. ⚠️ The ShowcasePage texts schema gained `articlesTitle`: restart `next dev` (see the customisation entry). Verified: tests (rules, service, 5 route guards), tsc, lint, build, and a 74-check browser run (write → private preview → send → sent back with notes → resend → approve → public FR/EN, byline, disclaimer, card and menu link on the page, feature/unfeature in « Nouveautés », live edit re-reviewed, section hidden/shown, taken down, others refused): 73 passed; the one failure was a React hydration error on the first load of the professional's page after a dev restart, not reproduced in 3 reloads (same dev-only pattern as before).
- **2026-09-15** — **[fix] A session refund could be sent to Stripe twice, and a failed cancellation refund was only a log line.**
  - **Before.** Two paths refund a session paid by card: the admin refund (`POST /api/payments/refund`) and the automatic refund when a paid appointment is cancelled (`PATCH /api/appointments/[id]`). Both called `stripe.refunds.create` with no idempotency key and no claim.
    - A double click, a retry or two cancellations racing could refund twice.
    - On a cancellation, any Stripe error was logged and the cancellation went on as if nothing was owed.
  - **Fixed.** Both paths now go through `refundAppointmentPayment` (`src/lib/appointment-refund.ts`), which follows the organization-invoice pattern:
    1. The refund is claimed on the appointment first (`payment.refundRequest`). It is one conditional write: `payment.status: "paid"`, and either no request yet or the previous attempt failed.
    2. Stripe is asked with the key `apptref_<appointmentId>_<attempt>`.
    3. The real amount is recorded as « refunded » or « partially refunded ». The receipt is voided only on a full refund.
  - **Stripe outcomes.**
    - A definitive Stripe refusal marks the attempt failed; the next attempt gets a new key.
    - An unknown outcome (network, Stripe 5xx) keeps the claim, so nothing is re-sent blind. After 10 min, the next call lists the payment's refunds and adopts the one carrying that attempt, or marks the attempt failed.
    - The webhook (`charge.refunded`, `charge.refund.updated`) closes the request too.
  - **Admin route answers.** 409 `NOT_REFUNDABLE` or `REFUND_IN_PROGRESS`, 400 `STRIPE_REFUSED`, 502 `REFUND_UNCONFIRMED`.
  - **Cancellation.** The cancellation stands whatever Stripe says. When the refund is refused or unconfirmed:
    - the team gets the new alert « Remboursement d'une séance à vérifier » (`admin_appointment_refund_problem`, set in Admin → Paramètres);
    - the client gets no « remboursé » email.
  - **Moved.**
    - The fee math is now in `src/lib/cancellation-refund.ts`. The 15 % fee is kept only on a late cancellation recorded as the client's; professional and team cancellations refund in full.
    - `isDefinitiveStripeError` is now in `src/lib/stripe-errors.ts`, shared with organization refunds.
  - **Tests.**
    - `appointment-refund.spec.ts`: claim filter, key per attempt, race, refusal, unknown outcome, adoption. 8 mutants killed.
    - `cancellation-refund.spec.ts`.
    - The refund route spec.
    - A « refund on cancellation (money) » block in `route.auth.spec.ts`.
  - **Not changed.** An appointment refunded before this fix has no `refundRequest`; it is recognised by `payment.status` alone.
- **2026-09-15** — **[feature, live on merge] « Nos professionnels » at www /professionnels (owner's request, inspired by a clinic's team page).**
  - **The page.** Every active professional is listed with a round portrait, their name, degree and title, and a short text. « Lire plus » links to the professional's page when they have a published one (owner's choice: every professional, not only those with a page).
  - **Where it is linked.** In the header, inside the « À propos » menu (desktop and mobile), the footer's Espace column, a band on the home page and « Qui sommes-nous », and the sitemap.
  - **Who is listed.** Rules live in `src/lib/professionals-directory.ts` (pure, spec'd); the loader is `professionals-directory-queries.ts`.
    - Professionals with `role: "professional"` and `status: "active"`.
    - A professional who unticked « Profil visible aux clients » (`Profile.profileVisible`) is never listed, even with a page.
    - A professional without a published page is listed only once their profile is completed.
  - **What each row shows.**
    - With a published page (switch on, a usable slug and a registry city): the page's reviewed name, portrait and text (intro, else bio, else headline, in the visitor's language), plus the link.
    - Without one: the profile's `bio` (French only, never reviewed by the team), the title and a short degree from `education`, initials instead of a photo (no profile photo may be public), and no link.
    - Texts are cut at 420 characters. Rows are sorted by last name. The row shape is pinned by `DIRECTORY_PROFESSIONAL_KEYS`.
  - **⚠ Visible in production on merge, whatever `showcaseEnabled` says.** Every completed, visible professional's name and profile bio become public. The only opt-out is the professional's own setting. Its wording now says so (« … et apparaître dans « Nos professionnels » sur le site »), but no professional has been told.
  - **Slugs.** `professionnels` moved from the reserved « pages to come » group to Routes.
  - **Not done.**
    - The team cannot hide a professional or pick the order.
    - The profile bio is not reviewed before it shows.
    - `profileVisible` still hides nothing else anywhere public (as before).
- **2026-09-15** — **[feature, live on merge] The team hides professionals from « Nos professionnels » and sets the order (owner's « lets do this »).**
  - **Screen.** Admin → « Nos professionnels (site) » (`/admin/dashboard/professionals-directory`, `manageProfessionals`, under « Pages vitrines ») lists every active professional in the public order, with a status for each:
    - « Affiché »;
    - « Masqué par l'équipe »;
    - « Masqué par le professionnel »: unticked « Profil visible aux clients », and it wins over the team;
    - « Profil incomplet ».
  - **Actions.** Move up and down, « Masquer » / « Afficher », « Enregistrer », and « Revenir à l'ordre alphabétique ».
  - **Stored in `PlatformSettings.professionalsDirectory`.** It holds `{order, hidden, updatedAt, updatedBy}`, absent until the first save.
    - A save replaces both lists whole and drops ids that are not active professionals.
    - It must carry the `updatedAt` the screen loaded. Otherwise it changes nothing and answers 409 `CHANGED`, so two admins never overwrite each other silently. A missing settings document answers 409 `SETTINGS_MISSING`.
  - **Order rule** (`src/lib/professionals-directory.ts`). Placed professionals come first in the team's order; everyone else follows by last name. Saving fixes the whole list, so a new professional shows at the end until moved.
  - **Route.** `GET/PUT /api/admin/professionals-directory`. Validation is `parseDirectoryCuration`: two lists of distinct ObjectIds (at most 2000 each) and the version, refused whole otherwise.
  - **Tests.**
    - Rules and validation: `professionals-directory.spec.ts`.
    - Route: `route.spec.ts`.
    - `showcases-guard.spec.ts` now also walks this route (401/403, nothing touched).
    - Local end to end: hide + reorder show on the public page at once; a stale save gets 409; professionals and visitors are refused.
  - **Not done.** No history of who changed what beyond `updatedBy` and a server log line. The profile bio is still shown unreviewed.
- **2026-09-15** — **[fix] « Nos professionnels » showed typed words as degrees and titles as bios (seen in production right after PR #12).**
  - **Degrees.** Profiles hold free text: the first degrees of the listed professionals were « Master », « Maitrise » and two sentences. The page read « Master, Psychologue », so `degreeOf` now shows only an abbreviation:
    - dotted: « Ph.D. », « M.A. », « Ph. D. », « B.Sc. »;
    - or two or more capitals: « PhD », « MSc », « MBA »;
    - it takes the first such entry, else none.
  - **Summaries.** A bio made only of title words (« Psychothérapeute », « Psychologue Psychologue scolaire Psychothérapeute ») now shows no summary instead of repeating the title (`isTitlesOnly`).
  - **Not changed.** What professionals wrote stays as is; they can write a real bio in their profile.
- **2026-09-16** — **[change] « Nos professionnels » redesigned (owner: « take it to the next level », then « keep the layout of circled image and description »).** The page keeps its rows — a circled portrait beside the presentation, alternating sides — in the professionals' pages' own type and palette (Fraunces + Plus Jakarta, sand `#F6F3EE`, teal `#17505F`), at their measure: content up to 1600 px with gutters growing to 96 px.
  - **Hero.** Eyebrow, large serif title (up to 92 px), lead, « Trouver mon professionnel » (/appointment) and « Nous contacter », three figures (professionals, languages spoken, combined years) and, from `lg`, an overlapping cluster of the team's portraits and monograms. The glows sit on one layer at the top of the page, so none meets a clipped edge.
  - **Rows.** Portrait 240 px to 352 px with a teal ring and a halo, or a tinted monogram (`directory-tints.ts`, taken in turn). Beside it: degree and title, the name (up to 48 px), years of experience, the text, then languages and ways of consulting as chips, and « Voir le profil » only when the professional has a published page. The text stays left-aligned on every row; only the portrait changes side.
  - **Filter.** Chips by profession, counted, in `ProfessionalsDirectoryGrid` (the page's only client component). Hidden with fewer than two professions.
  - **Closing.** A teal band leading to the matching questionnaire, plus the contact line.
  - **New on each listed professional** (`professionals-directory.ts`): `languages` and `modalities`, read from the profile's own words through the showcase's `showcaseLanguageKey` / `showcaseModalityKey` (so « Video Call », « Vidéo » and « En personne » all land right), and `yearsOfExperience` (a whole 0–70). The keys the page may read stay pinned by `DIRECTORY_PROFESSIONAL_KEYS`.
  - **Checked:** 400 px, 1440 px and 1920 px with no horizontal scroll, both languages, and professionals with and without a portrait.
- **2026-09-16** — **[change] A professional's page separates « Ce que j'accompagne » from « Champs d'expertise » (owner's request).** The two used to share one section, one title and one intro, with the professional's accompaniment cards stacked above the catalogue areas.
  - **Now two sections.** « Ce que j'accompagne » keeps the professional's own cards, their customisable title and intro (`expertisesTitle`, `expertisesIntro`) under a new eyebrow (`vitrine.focus.eyebrow`). « Champs d'expertise » follows on the sand ground with its own heading (`vitrine.expertises.title` / `.intro`) and the numbered catalogue cards, now white on that ground.
  - **Both grids fill their rows.** `FOCUS_GRIDS` and `EXPERTISE_GRIDS` pick the column count from how many cards there are (three across for three, two rows of three for six), instead of a two-column grid that left a lone card on its own row.
  - **Unchanged:** still one section key (`expertises`), so a professional's saved order and hidden sections keep working, and a page with only one of the two shows only that one.
- **2026-09-16** — **[change] A professional's page reads calmer on a large screen (owner: « the page is too zoomed »).** The type scale grew with the viewport until the name hit 99 px and section titles 65 px on a 1920 screen.
  - **Now:** the name is `clamp(40px,3.4vw,74px)` (48 px at 1440, 65 px at 1920), section titles `clamp(30px,2.3vw,52px)`, the headline, method and card titles and the closing band scale down with them, body text tops out at 20 px and the fluid steps (`vt-sm`, `vt-md`, `vt-lg`) rise more slowly.
  - **Sections keep their rhythm:** vertical padding `clamp(56px,5vw,104px)` instead of `clamp(64px,7vw,150px)`, so a large screen shows more of the page at once.
  - **Measured, not guessed:** computed sizes before and after at 1280, 1440 and 1920.
- **2026-09-16** — **[prod data] The demonstration page in production now shows the same sections as the local one (owner: « make sure it's completely the same, just to show the client even if it's not working »).** Backup `jechemine-20260916T140736Z.archive.gz` first; script `prod-demo-match-local.cjs` (dry run, then `--apply`, and a `--revert` that removes exactly what it added).
  - **Six catalogue expertises created** (Anxiété, Épuisement professionnel, Deuil, Estime de soi, Relations difficiles, Sommeil), `category: "expertise"`, `showcase: true`, `active: true`, and linked to the demonstration page's draft and published `expertiseIds`. **They are platform-wide**: `procatalogitems` was entirely empty in production, so these are the first entries the team and every professional will see in Admin → Mandats · Approches · Expertises. Renaming or deactivating them is the team's call.
  - **Two demonstration products created** (a 25 $ PDF and a 45 $ webinar), owned by the demonstration professional, `listInLibrary: false` so they stay off « Nouveautés ».
    - **They cannot be bought, and not because of a flag.** `/api/resources/[slug]/purchase-intent` loads the FR **and** EN rows and answers 404 unless both exist and are published; only the FR row was created. Verified live: both slugs answer 404. Their `previewHtml` says « Exemple de démonstration : ce contenu n'existe pas et n'est pas en vente. », which is what a visitor reads on `/book/<slug>` above the buy button.
    - `syncProductLiveStatus` and `reconcileProductLiveStatus` both go through `loadPair`, which returns null without an EN row, so the products job leaves them alone rather than failing.
  - **Deliberately still different from the local page:** « Disponibilités » shows « Aucune consultation ouverte en ce moment », because `services.standard/quick` stay false and `acceptingNewClients` stays false. Turning them on would make the page bookable, and a booking on it would take real money and enter the matching cascade for a professional who does not exist. The name also keeps « (exemple) ».
- **2026-09-16** — **[prod data] The demonstration professional removed from production, completely (owner's request, same day she was added).** Backup `jechemine-20260916T145344Z.archive.gz` first; `prod-demo-remove.cjs`, dry run then `--apply`.
  - **Deleted, all keyed on her single user id** (`6aaa8d61e8cca818ceb81bcd`, resolved from `demo.amel.sassi@jechemine.ca`): the account, her profile, her showcase page, her portrait `StoredFile` and her two demonstration products. An exhaustive re-scan of every collection finds no remaining mention of her.
  - **Never matched on a name.** Production holds real people called Sassi — an admin, a professional and another admin, plus their appointments, external messages and stored files. A name-based delete would have taken them. The script resolves one id from the email, refuses if the email is not unique, and refuses outright if any appointment, purchase or entitlement is attached.
  - **A portrait is deleted only when no other page points at it**, checked against every other `ShowcasePage`'s draft and published photo ids.
  - **Left in place on purpose, for the team to decide:**
    - the six catalogue expertises (Anxiété, Épuisement professionnel, Deuil, Estime de soi, Relations difficiles, Sommeil) — `procatalogitems` was empty before them and six real professionals already carry those words in `problematics`, so they are platform vocabulary now, not hers;
    - `showcaseEnabled: true` — Benjamin Lalonde, a real active professional, has a draft page behind it. With her page gone, production has **no** published showcase page.
  - **Verified after:** `/demo-amel-sassi` and `/book/demo-*` are 404; `/`, `/professionnels`, `/book` and `/appointment` are 200; `/professionnels` no longer names her; users 36 → 35, appointments unchanged at 41, the three real Sassi accounts intact.
